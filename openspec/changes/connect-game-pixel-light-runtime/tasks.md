## 1. 运行时配置解析

- [x] 1.1 梳理当前 `GameEngine`、`GameContext`、`EventPusher`、编码器和 `TcpLedOutputManager` 的输出边界，并记录真实控制器帧结构；用现有后端测试确认地砖输出基线
- [x] 1.2 新增外围灯运行配置模型和解析器，将 `commonConfig.pixelLightWiring` 转为本局不可变 profile；用单元测试覆盖启用、关闭、缺失字段和历史配置
- [x] 1.3 实现控制器索引、通道范围、重复通道、墙面 rows、起点、方向和 RGB 长度校验；验证非法 profile 被禁用并返回可诊断原因

## 2. 逻辑灯序列与报文映射

- [x] 2.1 实现根据四面墙数量、`selectedRow` 和 `order` 生成稳定逻辑灯序列；用顺时针、逆时针和不同起点的 golden 测试验证索引结果
- [x] 2.2 实现符号灯状态到 `pixelPort` 的 RGB payload 映射，并验证界面 1-based 值已正确转换为后端 0-based 通道
- [x] 2.3 实现圆灯状态到 `circlePort` 的 RGB payload 映射，并验证圆灯与符号灯通道互不覆盖
- [x] 2.4 在当前编码器/输出抽象中增加多控制器、多通道外围灯帧支持；用固定输入和 golden 报文测试确认不改变既有地砖字节

## 3. 游戏生命周期接入

- [x] 3.1 在游戏开始时冻结当前 profile，并让游戏帧输出使用该 profile；用假输出目标验证编辑器配置不会在运行中改变本局映射
- [x] 3.2 将符号灯、圆灯输出接入现有输出节奏，同时保持地砖矩阵发送路径和顺序；运行地砖回归测试确认原有帧不变
- [x] 3.3 为自然结束、手动结束、准备取消、启动失败、运行时异常和下一局切换接入统一清理入口；用生命周期测试验证清理幂等
- [x] 3.4 处理单个控制器/通道发送失败，记录游戏、控制器、通道和原因，并验证不会阻断其他目标或地砖游戏

## 4. 诊断与虚拟输出验收

- [x] 4.1 增加外围灯配置错误、编码失败和发送失败的结构化日志；用错误场景测试确认日志包含必要上下文
- [x] 4.2 使用假输出目标完成单控制器、多控制器、通道冲突、布局关闭和清理场景的集成测试
- [x] 4.3 使用假输出目标捕获单符号灯、单圆灯、多控制器和游戏结束清理报文，确认与 golden 映射一致；本 Change 不要求真实灯具
- [x] 4.4 执行完整游戏回归测试，确认没有外围灯配置的旧游戏、调试模式和现有地砖玩法均保持原有行为
