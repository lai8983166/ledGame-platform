## 1. 流程基础与视觉系统

- [x] 1.1 定义 `home`、`phone`、`register`、`swipe`、`success` 页面状态及头像弹层、头像库和软键盘 overlay 状态
- [x] 1.2 建立包含 phone、name、birthday、gender、avatarId 和模拟手环状态的 TypeScript 会话模型及统一重置动作
- [x] 1.3 定义深蓝背景、青色能量光、科技面板、字体、间距、阴影、状态色和触控尺寸等设计 token
- [x] 1.4 实现统一的 kiosk 壳层、步骤标题、背景网格/光效、科技面板、主次按钮和状态反馈组件
- [x] 1.5 准备约 18–24 个仓库内本地预置头像及头像元数据，不依赖远程图片

## 2. 首页与手机号确认

- [x] 2.1 实现 `Self-Service` 首页及 `Activate Wristband`、`Player Info Query` 两个触控卡片
- [x] 2.2 将 `Activate Wristband` 连接到手机号确认界面，并保持 `Player Info Query` 为不扩展后续流程的次要参考入口
- [x] 2.3 实现手机号输入面板、返回、确认、格式提示和错误状态
- [x] 2.4 实现数字软键盘的数字、退格、清空、关闭与 Done 操作，并将输入同步到电话号码字段
- [x] 2.5 验证合法手机号进入注册资料页并预填 Phone Number，无效号码停留并显示就地错误

## 3. 通用软键盘

- [x] 3.1 实现可复用软键盘组件及 numeric、alphabetic 两种布局
- [x] 3.2 实现字母键盘的字母、空格、退格、清空、关闭与 Done 操作
- [x] 3.3 实现输入字段焦点与键盘目标字段绑定，切换字段时同步切换布局和高亮状态
- [x] 3.4 键盘打开时压缩或移动页面内容，确保当前输入字段与关键操作不被遮挡
- [x] 3.5 保留物理键盘输入、退格和 Enter/Done 行为，并补齐键盘按钮的可访问名称

## 4. 注册资料表单

- [x] 4.1 实现 Avatar、Name、Phone Number、Birthday、Gender 字段和 Back、Next Step 操作
- [x] 4.2 将 Name 连接到字母键盘，将 Phone Number 与生日年/月/日连接到数字键盘
- [x] 4.3 实现性别大尺寸单选交互及唯一选中状态
- [x] 4.4 实现姓名、手机号、生日、性别和头像必填/格式校验，以及首个错误的聚焦或滚动提示
- [x] 4.5 验证返回手机号页或从刷卡页返回时保留当前会话资料

## 5. 头像选择

- [x] 5.1 实现 Avatar 编辑按钮和包含 `Set from Library`、`Take Photo`、取消操作的头像来源面板
- [x] 5.2 实现 `Take Photo` 的“UI 演示未接入相机”反馈，确保不调用任何相机 API
- [x] 5.3 实现 `Select an Avatar` 大尺寸头像网格、滚动区域和唯一选中视觉状态
- [x] 5.4 实现 `Confirm Avatar` 回填注册表单，并验证 `Cancel` 不改变原头像

## 6. 刷手环与成功结果

- [x] 6.1 实现 `Swipe Wristband` 页面、玩家头像/名称摘要、感应区域和等待状态
- [x] 6.2 实现环形脉冲或扫描提示动画，并在减少动态效果偏好下提供静态状态
- [x] 6.3 实现明确标注的 `Demo · Simulate Successful Swipe` 操作和短暂的已识别反馈，不访问真实硬件
- [x] 6.4 实现 `Activation Successful` 页面，展示成功图形、玩家摘要和 60 分钟有效时长
- [x] 6.5 实现 `Return Home`，一次性清空资料、错误、头像弹层、键盘和刷卡状态并返回首页

## 7. 自助机适配与验证

- [x] 7.1 为常见 16:9 横屏、较窄横屏和竖屏/极窄视口补齐布局适配或旋转提示
- [x] 7.2 检查所有主操作、头像和软键盘按键的触控尺寸、间距、按下、聚焦、禁用与错误状态
- [x] 7.3 检查科技面板上的标题、说明、表单和状态对比度，确保反馈不只依赖颜色
- [x] 7.4 执行 registration-kiosk 类型检查和生产构建并修复全部错误
- [ ] 7.5 在浏览器按七张参考图逐步验证首页、手机号、软键盘、注册、头像来源、头像库、刷卡、成功和返回重置流程
- [x] 7.6 验证流程中没有网络请求、存储写入、相机权限请求或读卡器/IC 卡硬件访问
