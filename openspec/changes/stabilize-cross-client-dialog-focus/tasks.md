## 1. 回归测试基线

- [x] 1.1 为共享 modal/drawer 的 opener 记录、有效节点恢复和失效节点跳过补充测试，并验证测试在旧实现上暴露缺口
- [x] 1.2 为会员管理端手环清零与回收的应用内确认行为补充测试，并验证源码不再允许裸 `window.confirm`
- [x] 1.3 为自助注册端软键盘重复唤起和刷卡监听避开可编辑控件补充测试，并验证旧实现上的失败断言

## 2. 共享弹层焦点生命周期

- [x] 2.1 实现可复用的 opener 捕获与安全恢复逻辑，并通过共享焦点单元测试
- [x] 2.2 将 `BaseModal` 和 `SideDrawer` 接入统一焦点生命周期，并通过组件测试与类型检查

## 3. 会员管理端确认交互

- [x] 3.1 将手环清零和回收改为共享应用内确认 modal，并通过取消、确认与失败分支测试
- [x] 3.2 扫描会员管理端 renderer，确认不存在未封装原生确认框并通过 member-admin 测试

## 4. 自助注册端输入治理

- [x] 4.1 关闭软键盘时 blur 活动输入并支持同一输入框再次点击唤起，通过键盘回归测试
- [x] 4.2 在刷手环 keydown 入口隔离可编辑事件目标及互斥弹层，通过读卡器与员工密码输入测试

## 5. 综合验证

- [x] 5.1 运行平台相关测试、类型检查和构建并修复所有回归
- [x] 5.2 运行 `openspec validate stabilize-cross-client-dialog-focus --strict` 并确认 Change 校验通过
