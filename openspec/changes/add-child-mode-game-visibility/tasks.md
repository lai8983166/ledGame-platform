## 1. 会员平台持久化设置

- [x] 1.1 先新增功能设置 service/API 测试，覆盖数据库缺省关闭、保存、重启读取和失败响应，并确认实现前测试失败
- [x] 1.2 新增 `store_feature_settings` schema、迁移兼容和 revision 跟踪，实现读取及儿童模式更新接口，并运行平台后端相关测试
- [x] 1.3 扩展操作审计映射，使真实儿童模式 PUT 记录系统设置变更，并用集成测试验证操作账号上下文

## 2. 房间长连接下行同步

- [x] 2.1 先扩展平台房间 WebSocket 测试，覆盖 `WELCOME` 当前设置、多在线 session 广播、失效连接隔离和旧协议上行不回归
- [x] 2.2 在现有房间连接注册表和 handler 中实现 `childMode` 下发与 `CHILD_MODE_CHANGED` 广播，并运行平台房间测试
- [x] 2.3 先扩展游戏后端房间客户端测试，覆盖握手、在线变更、未知消息、漏消息后重连覆盖及默认关闭
- [x] 2.4 在游戏后端实现线程安全功能设置投影并接入现有重连客户端，收到变更后通过现有运行时广播通知界面，运行房间客户端与运行时状态测试

## 3. 游戏可见性策略

- [x] 3.1 先扩展游戏后端目录与准备流程测试，覆盖儿童模式开启隐藏 `simple-demo`、关闭恢复、其他顺序不变及确认前竞态拒绝
- [x] 3.2 在可玩游戏目录集中实现受限游戏策略，并在新局选择和确认边界复核当前策略，运行目录和启动流程测试
- [x] 3.3 先扩展游戏前端目录/运行时测试，覆盖功能设置归一化、设置变化刷新和当前受限选择失效
- [x] 3.4 修改游戏模式选择页以消费运行时功能设置并重新加载列表，受限选择失效时安全返回待机；运行 `pnpm test` 与 `pnpm build`

## 4. 会员管理端真实功能开关

- [x] 4.1 先扩展共享 API 客户端和系统设置 UI 测试，覆盖真实 GET/PUT、加载中、保存成功、失败时保持原值和所有演示开关/说明消失
- [x] 4.2 扩展共享 API 类型与客户端方法，修改功能开关页只展示默认关闭且持久化的儿童模式，并运行客户端测试、类型检查和构建

## 5. 跨端验证

- [x] 5.1 通过平台 WebSocket 集成测试和游戏后端行为测试验证两端使用同一个 `childMode` 契约
- [x] 5.2 运行平台后端/客户端、游戏后端及游戏前端相关完整测试，执行 `openspec validate add-child-mode-game-visibility --strict` 并记录验证结果

## 6. 消融与设计收紧

- [x] 6.1 删除只为未来扩展预留的通用设置快照包装和未消费的更新时间字段，将跨端契约收紧为单一 `childMode` 布尔值
- [x] 6.2 删除只检查源码字符串的跨仓库契约测试，保留平台 WebSocket 集成测试、游戏后端行为测试和游戏前端流程测试
- [x] 6.3 重新运行相关测试、构建和 OpenSpec 严格校验，确认简化前后业务行为一致
