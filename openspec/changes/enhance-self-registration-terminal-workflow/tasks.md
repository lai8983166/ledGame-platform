## 1. Player Info 查询契约

- [x] 1.1 扩展 Spring Boot `/api/player-info`，要求 `phone` 与 `wristbandUid` 二选一，并复用会员、积分、排名、手环余额和近期游玩记录聚合。
- [x] 1.2 扩展 `packages/api-client` 的 Player Info 类型和调用方法，保留手机号字符串调用兼容性并增加参数校验测试。
- [x] 1.3 手环查询保持只读，只接受当前有效绑定，不激活、绑定、启动计时或修改手环状态。

## 2. 自助注册端会员信息查询

- [x] 2.1 `playerInfoFlow` 支持手机号和手环 UID 两种模式、格式校验、失败状态和重置。
- [x] 2.2 Player Info 页面提供模式切换、软键盘输入、读卡器扫描查询和取消路径；扫描查询不调用绑定接口。
- [x] 2.3 结果页展示真实会员资料、积分总数、当前排名、手环余额和近期游玩记录，正确处理空记录与错误状态。
- [x] 2.4 离线遮罩只提示会员管理端网络连接问题和网络检查建议；断线时清理当前会话，恢复后可重新开始。

## 3. 流程倒计时与设置

- [x] 3.1 增加激活、刷手环、成功页和 Player Info 流程的倒计时配置模型、本地持久化、默认值和 10–600 秒校验。
- [x] 3.2 在 LED GAME 标题旁增加设置按钮和设置弹窗，保存失败或非法值时保留旧配置并显示错误。
- [x] 3.3 实现顶层流程计时器，进入每个非首页流程时重新计时并显示剩余秒数；切换流程、打开弹窗或读卡时不会重复叠加计时器。
- [x] 3.4 超时返回首页，取消未完成的读卡/API 流程并清理临时状态，不产生数据库变更。

## 4. 集成验证与交付

- [x] 4.1 更新核心流程和 Player Info 集成测试，覆盖手机号注册、手环查询、积分排名、游玩记录、只读过期查询、离线恢复和倒计时模块。
- [x] 4.2 运行服务端、共享包、API client、自助注册端单元/契约测试及类型检查。
- [x] 4.3 构建自助注册端生产包并检查设置入口、双模式查询、离线提示、倒计时返回首页和个人信息清理。
