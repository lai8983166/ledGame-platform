## 1. 先建立失败测试与契约

- [x] 1.1 为自助注册端增加默认头像、男性/女性头像过滤、性别切换兼容性测试，并先验证新断言在现有实现上失败
- [x] 1.2 为刷手环按钮、等待弹层、数字加回车、取消、迟到响应和非等待输入增加前端测试，并先验证现有输入框交互不能通过这些测试
- [x] 1.3 为日/月/年周期边界、仅统计 `COMPLETED`、空榜和并列竞赛名次增加真实 SQLite 集成测试，并先验证平台缺少排行榜接口时测试失败
- [x] 1.4 为共享客户端契约和会员管理端周期请求、刷新、空状态、错误状态及删除演示排行增加测试，并先验证现有排行榜实现不通过
- [x] 1.5 将新增测试纳入 `pnpm test:client` 或对应聚合脚本，并验证单条命令能够发现和执行全部新增前端测试

## 2. 实现注册头像规则

- [x] 2.1 为内置头像目录增加稳定 ID、`male`/`female`/`neutral` 适用范围和各范围默认值，并通过头像目录单元测试
- [x] 2.2 让新注册会话初始化真实默认 `avatarId`，返回首页时正确重置，并通过默认头像直接提交与会话隔离测试
- [x] 2.3 根据性别过滤头像库，在不兼容时自动改选对应默认头像、兼容时保留选择，并通过男/女/不透露及性别切换测试

## 3. 实现显式刷手环会话

- [x] 3.1 用“开始刷手环”按钮和可取消等待弹层替换顾客可见 UID 输入框，并通过页面初始状态与可访问操作测试
- [x] 3.2 实现仅在等待状态生效的数字 UID 加回车捕获、缓冲清理和重复提交保护，并通过成功、取消、非等待输入及连续扫描测试
- [x] 3.3 让查询或绑定失败留在可重试弹层，成功自动解除提示，同时使用扫描会话标识忽略取消后的迟到响应，并通过异步状态测试
- [x] 3.4 更新受影响的自助注册端文案、样式和稳定测试选择器，并验证 `pnpm typecheck:registration` 与 `pnpm build:registration` 通过

## 4. 实现真实周期排行榜服务

- [x] 4.1 在共享 API 客户端中定义排行榜周期、请求函数和响应条目类型，并通过客户端契约测试及 `pnpm typecheck:packages`
- [x] 4.2 在平台服务端使用可注入 `Clock` 和明确 `ZoneId` 计算自然日/月/年的半开时间区间，并通过固定时钟的边界测试
- [x] 4.3 新增只读排行榜查询，聚合有效会员的周期内已结算积分和完成局数，实现同分竞赛名次与稳定排序，并通过真实 SQLite 集成测试
- [x] 4.4 为非法周期返回稳定参数错误并确认接口响应包含周期边界与生成时间，通过服务端控制器测试和 `pnpm test:server`

## 5. 会员管理端接入排行榜

- [x] 5.1 移除排行榜页面对本地 `rankings` 演示数组和 `trend` 的依赖，改为调用共享客户端，并通过“无演示回退”测试
- [x] 5.2 实现进入页面、周期切换、手动刷新和迟到响应隔离，并通过请求状态测试验证当前周期不会被旧响应覆盖
- [x] 5.3 实现加载、错误重试、空榜、少于三人和成功榜单展示，使用服务端生成时间，并通过管理端组件测试
- [x] 5.4 让副屏预览复用当前真实榜单且保留“本机 UI 预览”说明，并验证 `pnpm typecheck:member-admin` 与 `pnpm build:member-admin` 通过

## 6. 核心流程回归与验收

- [x] 6.1 更新核心验收脚本，使自助端先点击刷手环按钮，再输入测试 UID `2283055618` 和回车完成绑定，并通过相关 Playwright 场景
- [x] 6.2 运行 `pnpm test:core`、`pnpm typecheck` 和 `pnpm build`，修复全部回归并保存测试结果
- [x] 6.3 运行 `pnpm test:e2e`，确认会员创建、手环充值与绑定、游戏结算、Player Info 和排行榜积分来源保持一致
- [x] 6.4 执行 `openspec validate improve-kiosk-registration-and-real-leaderboard --strict`，确认规格、实现任务和最终勾选状态可归档
