## 1. 测试骨架与配置边界

- [x] 1.1 为备份能力增加独立配置属性、状态模型和错误码，并验证测试配置默认使用隔离目录且不会探测或修改日常开发数据库
- [x] 1.2 先编写磁盘拓扑与目标选择的失败测试，覆盖同盘不同分区、真正异盘、数据库盘不同于安装盘、多个候选、无候选、只读盘、网络盘和空间不足
- [x] 1.3 先编写真实 SQLite 备份的失败集成测试，覆盖提交后备份、并发写入、一致性检查、失败不替换 latest、最新数据最终可见和测试目录清理
- [x] 1.4 先编写调度与状态的失败测试，覆盖 5 秒触发时限、持续写入不饥饿、掉盘降级、重新接入恢复、每日快照和 30 天清理
- [x] 1.5 先编写数据库身份/revision、启动门禁和退出收尾的失败测试，覆盖提交与回滚、主库领先、版本相同、备份领先、身份冲突、主库损坏、降级启动、正常关闭和强制终止后追赶
- [x] 1.6 先编写导入功能的失败测试，覆盖未登录/次级账号拒绝、出厂账号授权、启动窗口无入口、候选发现与校验、版本和最后修改时间预览、取消、营业中拒绝、成功覆盖、旧版本导入、登录失效、替换失败回滚和导入中强制终止恢复

## 2. Windows 物理磁盘识别

- [x] 2.1 实现可替换的磁盘拓扑探测接口和模拟实现，并通过 1.2 的纯单元测试
- [x] 2.2 实现基于 PowerShell/CIM 结构化 JSON 的 Windows 卷—分区—物理磁盘探测器，并用解析测试验证中文系统环境下不依赖本地化文本
- [x] 2.3 根据 `platform.db` 规范化路径解析源物理盘，使用 UniqueId、序列号和运行期磁盘编号的回退顺序，并验证同一物理盘的其他分区全部被排除
- [x] 2.4 实现候选资格检查、可写探测、空间安全余量和确定性排序，并验证自动生成固定目录 `LEDGameBackup/member-admin`
- [x] 2.5 持久化上次成功目标的非敏感身份并实现重新发现逻辑，验证盘符变化、目标掉线和重新接入时无需管理员选择即可恢复

## 3. 数据库版本与启动/退出门禁

- [x] 3.1 增加数据库实例身份、单调 revision、最后业务修改时间和导入来源状态表及各受保护业务表的版本触发器，并用 schema 契约测试验证新增/修改/删除、事务回滚和新增业务表不得遗漏触发器
- [x] 3.2 实现主库与备份的身份/revision 比较器，并验证相同、主库领先、备份领先、身份冲突和旧备份缺少版本信息的判定均符合规格
- [x] 3.3 实现后端启动检查协调器和 `CHECKING`、`READY_PROTECTED`、`READY_DEGRADED`、`MAINTENANCE_LOGIN_REQUIRED`、`BLOCKED` 门禁，验证检查完成前持久化请求得到稳定重试错误，冲突状态只开放登录/出厂维护，且只读启动状态可访问
- [x] 3.4 实现 Electron 独立启动检查窗口及阶段进度展示，验证窗口不包含候选路径或导入入口；受保护/降级结果进入正常登录，备份领先、身份冲突或重装空库进入受限登录，主库损坏保持阻断且不出现空白窗口
- [x] 3.5 实现正常退出的有界 shutdown flush 与“正在保存并备份数据”状态，验证提交后立即关闭仍备份最后版本、目标盘故障不会无限等待、强制终止后由下次启动追赶

## 4. SQLite 一致性备份引擎

- [x] 4.1 使用现有 Xerial SQLite 在线 Backup API 生成 staging 候选数据库，并通过并发写入测试证明不使用主库文件直接复制且只包含完整事务
- [x] 4.2 对候选数据库运行独立只读 `PRAGMA integrity_check`、核心 schema 检查和 SHA-256 计算，并验证无效候选不会进入 latest
- [x] 4.3 实现同卷原子发布协议与断电残留 staging 清理，验证生成失败、校验失败和替换中断时上一份 latest 仍可打开
- [x] 4.4 生成 latest 与 history 配套元数据，包含数据库实例身份、revision 和最后业务修改时间，并验证时间、源身份、目标身份、文件大小、校验和和完整性结果齐全且可复核
- [x] 4.5 实现每日历史快照与默认 30 天保留清理，验证不会删除 latest、保留期内快照或仅存的有效历史副本

## 5. 自动触发、重试与诊断

- [x] 5.1 实现主数据库及 SQLite 辅助文件变化监测和定期文件指纹兜底，验证当前及未来未显式接入的数据库写入都能触发备份
- [x] 5.2 实现单任务串行、防抖、首次脏时间上限和备份后再次检查，验证事务提交后 5 秒内开始尝试且持续写入不会无限推迟
- [x] 5.3 实现启动追赶备份、每日无写入快照检查和失败退避重试，验证后端无需重启即可从目标盘不可用状态恢复
- [x] 5.4 将无异盘、拓扑探测失败、不可写、空间不足、在线备份失败、完整性失败、版本/身份冲突和发布失败映射为稳定错误码与中文操作建议，并验证备份失败不改变已成功的营业事务
- [x] 5.5 增加结构化服务日志和只允许本机管理调用的只读备份/启动状态接口，验证接口不泄露磁盘序列号等不必要信息且远程请求被拒绝

## 6. 备份数据库导入与安全替换

- [x] 6.1 为候选列举、外部文件登记、验证、导入准备和确认执行增加服务端 `FACTORY_ADMIN` 授权，并验证未登录与次级账号通过 API、IPC 或界面均无法获得候选路径或修改数据库
- [x] 6.2 实现 fixed latest/history 与出厂账号本地文件选择的候选发现、只读 SQLite/元数据/哈希/schema 兼容性验证，并验证无效或高版本候选无法进入确认步骤
- [x] 6.3 仅在出厂账号登录后的备份管理界面实现候选列表与确认弹窗，验证启动检查窗口和次级账号界面没有入口，正文准确显示“要将版本为 {version}，最后修改时间为 {lastModifiedAt} 的数据库导入吗？”、覆盖警告、取消和确认操作
- [x] 6.4 实现导入前营业状态检查与 `IMPORTING` 维护门禁，验证存在进行中游戏或排队时拒绝导入，确认后所有三端新写入被阻止
- [x] 6.5 实现 Electron 停止后端、创建 pre-import 回滚副本、候选同卷 staging、SHA-256 复核和原子替换流程，并验证数据库打开期间绝不直接覆盖主文件
- [x] 6.6 实现原子维护日志、导入后重启复验和失败自动恢复 pre-import，验证复制、替换、启动及完整性检查各阶段失败和强制终止都不会使用部分文件
- [x] 6.7 实现主动导入旧版本的 revision 推进、导入来源审计、旧 latest 归档、新 latest 生成和旧登录会话失效，并验证重启后不会误判回退且必须使用导入后账号重新登录

## 7. 会员管理端状态界面

- [x] 7.1 扩展会员管理端 API 客户端与状态轮询，验证能够展示备份中、正常、不可用和失败状态以及最后成功时间
- [x] 7.2 在设置页增加备份详情卡片，在其他管理页面增加紧凑降级告警，并通过 UI 测试验证目标卷和中文检查建议清晰可见
- [x] 7.3 验证降级备份告警不会阻断会员注册、充值、激活、结算、查询等既有操作，也不会造成 Electron 窗口切换后的输入焦点回归问题

## 8. 自动化验证与真实硬件验收

- [x] 8.1 将备份与导入单元测试、真实 SQLite 集成测试加入仓库标准测试命令，并验证一次命令执行不会接触开发桌面版和打包版共用的 `platform.db`
- [x] 8.2 增加 Windows 双物理硬盘只读识别/测试数据库备份与导入验收脚本，验证脚本能打印源物理盘、目标物理盘、固定目录和校验结果且不覆盖真实主库
- [x] 8.3 编写中文《数据库异盘备份、导入与恢复说明》，明确导入仅限登录后的出厂账号且启动检查窗口不提供入口，并包含版本与身份状态、启动检查、导入确认、退出收尾、目录结构、厂家离线恢复、恢复前校验和禁止运行中直接复制数据库的要求
- [x] 8.4 编写中文真实双硬盘验收清单，覆盖首次启动窗口、版本追赶、重装后导入、旧历史版本导入、导入失败回滚、关键业务写入后立即关闭、强制终止、拔盘、重新接入、空间不足和隔离恢复演练
- [x] 8.5 执行备份/导入专项测试、平台完整测试、桌面端测试和打包冒烟测试，并记录通过命令与结果；所有失败修复后重新执行受影响套件
  - `pnpm test:core`：通过（服务端完整测试、24 个前端测试文件 / 91 个用例、国际化检查）
  - `pnpm test:desktop`：通过（5 个测试文件 / 20 个用例，包含数据库原子导入与断电恢复）
  - `pnpm typecheck`：通过（三端共享包、会员管理端和自助注册端）
  - `pnpm test:desktop:member`：通过（1 个真实 Electron 用例）
  - `pnpm portable:member-admin:dir`：通过，已更新 `release/member-admin/win-unpacked`
  - `pnpm portable:smoke`：通过（两个 Windows 目录包）
  - `scripts/verify-database-backup-hardware.ps1`：PowerShell 语法检查通过；真实双物理盘、拔盘和断电验收按 8.4 清单在目标机器执行
  - `openspec validate add-automatic-cross-disk-sqlite-backup --strict`：通过

## 9. 生产/测试隔离与现场问题纠正

- [x] 9.1 先增加回归测试，复现临时 Electron 测试库写入正式备份目录、正式启动被测试备份阻断、错误候选覆盖账号、关闭主窗口后 8090 仍监听和重复启动的问题
- [x] 9.2 为 v2 备份元数据增加 `PRODUCTION`/`TEST` 环境，自动化测试使用唯一测试根目录真实生成备份，正式协调器与固定候选发现隔离或忽略无环境/异环境备份
- [x] 9.3 增加出厂账号“保留当前主库并重新建立备份”的服务端动作和管理端确认交互，验证冲突文件进入 quarantine、营业门禁解除且无文件被静默删除
- [x] 9.4 扩展候选预检与确认信息，要求唯一已启用出厂账号并展示候选出厂用户名、会员数量和导入后账号变化警告
- [x] 9.5 修复 Electron 启动窗口、主窗口关闭、单实例和托管 Java 进程退出时序，并用真实 Electron 测试验证关闭后端口释放、可立即重开且重复启动不会创建第二个后端
- [x] 9.6 更新中文说明与验收清单，执行专项测试、平台完整测试、桌面端测试、打包与冒烟测试，并重新严格校验 Change
  - `mvn -q -f server/pom.xml '-Dtest=BackupTargetSelectorTest,DatabaseBackupEngineTest,DatabaseBackupCoordinatorStartupTest,DatabaseImportServiceTest,DatabaseBackupControllerAuthorizationTest' test`：通过
  - `pnpm test:core`：通过（服务端完整测试、24 个前端测试文件 / 92 个用例）
  - `pnpm test:desktop`：通过（5 个测试文件 / 21 个用例）
  - `pnpm typecheck` 与 `pnpm i18n:check`：通过
  - `pnpm test:desktop:member`：通过（真实 Electron 验证 TEST 备份、单实例、动态端口切换与关闭释放端口）
  - 会员管理端独立验证目录打包与 `pnpm portable:smoke`：通过（会员管理端打包包真实生成隔离的 TEST v2 备份；两个 Windows 目录包均启动成功）
  - `scripts/verify-database-backup-hardware.ps1`：Windows PowerShell 语法检查通过；真实双物理盘验收按中文清单现场执行
  - `openspec validate add-automatic-cross-disk-sqlite-backup --strict`：通过

## 10. 自动化验收缺口补齐

- [x] 10.1 增加最终打包产物首次启动测试，验证独立检查窗口不泄露候选或导入入口、检查完成后进入登录页并生成带 TEST 标记的首次备份
- [x] 10.2 增加最终打包产物异常终止与重启追赶测试，验证 Electron/Java 被强制结束后主库已提交数据仍存在且 latest 最终追平主库
- [x] 10.3 增加空间不足、只读/不可写目标的服务级故障注入测试，验证稳定错误码、中文建议、营业门禁保持可用、重试恢复和旧 latest 不被替换
- [x] 10.4 增加临时 userData 下的完整重装导入自动化流程，覆盖空库不覆盖旧备份、维护登录、候选展示、取消、确认导入和导入后重新登录
- [x] 10.5 增加导入过程的真实子进程中断矩阵，分别在回滚副本已保存、主库已替换和等待启动复验时终止，并验证下次启动只使用完整旧库或完整新库
- [x] 10.6 补齐候选账号异常矩阵与权限链路测试，覆盖无出厂账号、出厂账号停用、多个启用账号，以及 API、IPC 和界面不可执行导入
- [x] 10.7 增加最终打包产物的正常退出、端口释放、立即重开和单实例测试
- [x] 10.8 将新增测试接入明确的仓库命令，执行专项、完整与 OpenSpec 严格校验，并更新中文验收清单中的自动化覆盖说明
  - `pnpm test:desktop:member:packaged`：通过；重新构建会员管理端 `win-unpacked` 后，3 个最终打包产物用例全部通过
  - `pnpm test:desktop`：通过（5 个测试文件 / 24 个用例，其中导入真实子进程中断矩阵 3 个节点）
  - `pnpm test:core`：通过（服务端完整测试、24 个前端测试文件 / 94 个用例）
  - `pnpm typecheck`：通过
  - `openspec validate add-automatic-cross-disk-sqlite-backup --strict`：通过
