## 1. 最小工具骨架与配置

- [x] 1.1 建立 `multipoint-concurrency` TypeScript/Vitest 工具目录以及 `center`、`agent`、`verify` 三个命令入口，并通过类型检查和空命令测试验证骨架
- [x] 1.2 先编写配置测试，覆盖 runId、agentId、平台地址、会员管理端程序路径、测试端口、并发数、持续时间、超时和安全确认值的有效与无效输入
- [x] 1.3 实现共享配置加载以及 `smoke`、`load` 两份中文模板，使配置测试通过且任何失败均发生在写请求之前
- [x] 1.4 先编写确定性数据生成测试，证明 B/C 使用不同 agentId 时手机号、UID、设备和外部会话编号跨大批量计划不冲突
- [x] 1.5 实现身份生成器，并验证相同 runId、agentId 与配置始终生成相同计划

## 2. 隔离启动打包版会员管理端

- [x] 2.1 先编写中心启动配置测试，验证每个 runId 获得独立 userData、日志、`TEST` 备份 root override 和默认测试端口，且正常数据路径不在任何测试目标内
- [x] 2.2 先编写启动预检测试，覆盖打包程序不存在、正常平台仍在线、测试端口占用和测试目录不可写，并验证工具不会结束未知进程或改用随机端口
- [x] 2.3 实现 `center` 命令，使用现有环境变量启动同一份会员管理端打包程序、等待健康检查并输出供 B/C 使用的连接配置文件
- [x] 2.4 先编写桌面测试，要求测试参数存在时启动检查窗口和主窗口显示“并发测试模式”及 runId，正常启动时不显示
- [x] 2.5 实现最小测试标识传递与展示，并验证数据库模式不能从已登录 UI 中切换
- [ ] 2.6 验证操作者正常关闭测试实例时现有退出备份会执行，随后正常启动会员管理端仍读取原正式数据库；不新增脚本进程终止逻辑

## 3. 两机并发代理

- [x] 3.1 先编写计划文件测试，覆盖完整计划先落盘、注册/游戏工作单元数量、序号和代理中断后的未完成判定
- [x] 3.2 实现简单 `plan.json` 与 `results.jsonl` 写入，保证一个结果能通过 operationId 回到唯一计划项
- [x] 3.3 先为会员查询/创建、手环充时/绑定、激活、单人开局、自然结算和最终查询编写现有 HTTP 契约测试
- [x] 3.4 实现无自动重试的平台客户端，记录 HTTP 状态、有限响应、耗时、超时、连接失败及业务身份
- [x] 3.5 先编写注册工作单元并发测试，覆盖“充时 → 查询或创建会员 → 绑定”以及前序失败时后续步骤跳过
- [x] 3.6 实现可配置数量的注册工作单元，并验证每个循环使用新的确定性会员和手环
- [x] 3.7 先编写游戏工作单元测试，覆盖独占会员/手环准备、激活、唯一会话开局、自然结算和确定性积分
- [x] 3.8 实现可配置数量的游戏工作单元，并验证同一工作单元串行游戏、不同工作单元与注册流同时运行
- [x] 3.9 实现 `agent` 中文摘要和退出码，验证计划未完成、非预期 5xx、超时及连接失败不会被误记为成功

## 4. 精确核账与中文报告

- [x] 4.1 先编写跨代理输入测试，覆盖缺少结果目录、runId 不一致、agentId 相同、计划身份碰撞和两台运行时间未重叠
- [x] 4.2 先编写核账表驱动测试，覆盖成功响应后会员缺失、错绑、游戏缺失、重复会话、未结算、积分错误以及无响应但数据已提交
- [x] 4.3 实现只读 `verify`，通过公开接口获取并索引会员、手环和游戏记录，按 operationId 逐项比较计划、请求结果与最终数据
- [x] 4.4 扫描本次隔离中心日志并统计非预期 5xx、`SQLITE_BUSY`、`database is locked` 和中心退出证据，验证分类不会依赖日志总行数
- [x] 4.5 计算吞吐量和 p50/p95/p99，并验证性能告警与数据正确性结论分开
- [x] 4.6 先编写中文报告快照测试，再生成 Markdown 与机器可读 JSON，内容包含两机重叠区间、计划/尝试/成功/失败、字段级差异、结论、数据目录和覆盖边界

## 5. 便携发布与验收

- [x] 5.1 增加便携构建脚本，只复制固定 Node 运行时、许可证、编译结果、中文配置模板和三个启动入口，并验证不携带 `node_modules`
- [x] 5.2 增加发布物自检，验证在不安装 Node、pnpm、JDK 的临时目录中能完成配置检查和离线计划生成
- [ ] 5.3 建立使用临时 userData、随机端口和隔离备份目录的本机集成测试，由 center 启动打包形态会员管理端并由两个 agent 完成 smoke，最终 verify 零差异
- [x] 5.4 增加受控失败集成测试，验证成功后数据缺失、代理中断、无响应但已提交、数据库锁错误和无重叠运行分别得到正确结论
- [x] 5.5 编写简洁中文三机说明：A 运行 center、复制连接配置到 B/C、分别设置 agentId、运行 agent、复制两份结果回 A、运行 verify、关闭测试实例并正常启动恢复正式库
- [x] 5.6 在文档和报告中明确测试备份 root override 不代表真实异盘验收，且直接 API 负载不覆盖 UI、游戏本地后端、房间 IP、控制器和地砖
- [ ] 5.7 在三台 Windows 机器上依次完成 smoke 和正式 load，保存第一份脱敏中文基准报告
- [x] 5.8 运行新增测试、平台现有 `test:core`、`test:e2e`、桌面测试和打包校验，再以严格模式验证本 OpenSpec Change
