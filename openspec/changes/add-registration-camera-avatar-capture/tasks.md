## 1. 契约与测试夹具

- [x] 1.1 定义可选头像载荷、媒体类型/尺寸/字节上限、稳定错误码和头像读取响应，先补 API 契约测试验证旧版只携带 `avatarId` 的请求仍然有效
- [x] 1.2 建立可注入的摄像头设备、权限和断流测试夹具，先编写无设备、单设备、多设备、拒绝权限、占用和拔出场景并确认实现前按预期失败

## 2. 自助注册端摄像头流程

- [x] 2.1 在 registration-kiosk 桌面主进程和 preload 中配置 `media` 权限请求/检查，并验证开发版与打包版的摄像头权限测试均能得到明确结果
- [x] 2.2 实现摄像头枚举、设备选择、预览流释放和错误码映射，运行摄像头夹具单元测试验证设备变化不会清除注册会话
- [x] 2.3 在头像来源弹窗中加入“拍摄头像”入口、预览、拍照、重拍、确认和取消状态，运行 kiosk UI 测试验证确认前不覆盖头像、取消不清空表单且无摄像头可继续使用头像库
- [x] 2.4 实现照片正方形裁剪、尺寸/字节限制、临时数据清理和可选注册请求载荷，运行边界测试验证超限文件被拒绝且成功/失败/取消后无临时残留

## 3. 会员管理端头像存储

- [x] 3.1 增加头像文件存储路径、随机标识和 AES-GCM 二进制加密封装，运行服务端单元测试验证认证失败、篡改、密钥错误和路径穿越均被拒绝
- [x] 3.2 扩展会员创建接口接收可选头像并执行 MIME、解码、像素和大小校验，运行集成测试验证头像文件与 `avatar_id` 原子写入、失败回滚和旧请求兼容
- [x] 3.3 增加受控头像读取及缺失文件回退逻辑，运行会员查询、Player Info 和管理端展示测试验证不暴露绝对路径、日志或照片正文
- [x] 3.4 扩展加密数据库备份、候选校验和导入回滚以携带头像文件清单及文件，运行备份/导入集成测试验证数据库与头像文件不会出现混合版本

## 4. 管理端与跨端集成

- [x] 4.1 更新 API 客户端和会员管理端头像展示模型，运行前端测试验证内置头像、已上传头像、文件缺失三种情况均有稳定回退
- [x] 4.2 完成自助注册端到会员管理端的端到端拍照注册流程，运行隔离 SQLite 核心流程测试验证会员、头像标识和既有手环流程均正确保存
- [x] 4.3 增加开发版/打包版摄像头权限、设备异常和重启测试，并验证旧版客户端请求仍可注册会员

## 5. 隐私、文档与交付验证

- [x] 5.1 扫描数据库、头像目录、WAL/SHM、临时目录和 server.log，验证没有照片明文、完整上传内容、绝对路径或数据密钥残留
- [x] 5.2 更新中文打包使用说明和维护说明，明确支持的摄像头类型、权限处理、故障回退、照片保存位置和不支持 IP 摄像头的范围
- [x] 5.3 运行会员管理端服务端、客户端、桌面端、核心流程、备份/导入和打包版回归测试，确认新增头像功能不改变会员注册、查询和手环核心流程
- [x] 5.4 运行 `openspec validate add-registration-camera-avatar-capture --strict`、类型检查和 `git diff --check`，确认本 Change 的所有任务与规范一致
