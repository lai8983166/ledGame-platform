## 1. 机器身份、签发和验签

- [x] 1.1 先定义授权格式和错误码并编写契约测试，覆盖格式／产品／机器不匹配、篡改、未知密钥、激活码长度超限、非法编码、空输入／空白清理和永久授权不依赖当前日期
- [x] 1.2 实现固定注册表视图的机器身份读取与版本化机器码生成，测试读取失败、规范化、网络／端口／目录变化不改变机器码，且日志不包含原始身份
- [x] 1.3 实现厂家密钥生成与签发命令行工具，用临时密钥验证签发和跨 Node／Java 验签一致、激活码文本可复制且 Node／Java 编解码一致
- [x] 1.4 实现后端授权服务及独立授权文件保存，使 1.1 测试通过，并测试无效激活码、写盘失败／中断不破坏原文件，成功落盘后才激活

## 2. 后端门禁与启动顺序

- [x] 2.1 先增加未激活 HTTP／房间连接门禁测试，证明登录、读写业务及结算不可绕过且拒绝不改变数据库，健康与授权诊断仍可用
- [x] 2.2 接入统一授权门禁与本机授权查询／激活码提交接口，验证稳定错误码、中文原因、loopback 限制和不接收任意服务器文件路径
- [x] 2.3 将激活作为备份启动检查的前置条件，集成测试覆盖授权通过后只执行一次备份检查、备份 READY 不覆盖未激活、维护仍需原出厂账号权限

## 3. 会员管理端启动界面

- [x] 3.1 复用启动窗口增加机器码复制、激活码输入、粘贴按钮、激活、失败原因、重试和退出；桌面测试覆盖 Ctrl+V／按钮粘贴、空剪贴板／读取失败可手输、空白清理、非法字符、不自动提交及失败后焦点可用
- [ ] 3.2 调整启动等待，使人工申请激活不触发后端启动超时；测试激活码提交成功自动继续备份／登录、关闭窗口正常释放自身 Java 进程和端口
- [ ] 3.3 验证同系统重装应用可重输原激活码，升级、更改端口和数据库备份导入不影响授权；使用隔离目录执行，不替换开发者现有数据库
- [x] 3.4 检查自助注册端和游戏端现有错误呈现／重连行为，用接口与现有客户端测试确认未激活不被误报为网络故障；若存在跨仓库提示缺口，记录具体位置并确认后再扩展实施范围

## 4. 测试隔离与发布

- [x] 4.1 更新开发和服务测试夹具使用临时密钥／身份注入，验证正式运行不存在环境变量或配置文件跳过验签／替换信任公钥的入口
- [x] 4.2 更新打包版验收和并发 center 启动器接受显式本机内部授权文件并复制到隔离目录（仅测试预置，客户流程仅输入激活码）；测试原文件不变、无授权明确失败、离线计划预览无需激活
- [ ] 4.3 配置客户应用与源码包的授权公钥和排除规则，发布校验验证私钥／测试信任材料不进入产物、公钥缺失时不生成临时正式密钥；厂家正式公钥由厂家提供
- [ ] 4.4 在已获厂家授权的测试机完成正式打包版首次粘贴激活、重启保持、无效激活码和隔离并发验收；没有有效厂家授权时明确保留此项未完成，不以测试绕过代替

## 5. 文档与整体回归

- [x] 5.1 交付简洁中文客户复制粘贴激活说明与厂家生成激活码／密钥保管说明，明确同系统重装、系统身份变化、旧版升级、永久授权和不可远程吊销的边界
- [x] 5.2 运行授权专项、后端核心、桌面启动／备份、并发工具及打包校验，记录命令与结果；严格验证此 Change，未执行的实机项保持未勾选

## 6. 厂家 Windows 图形签发工具（追加）

- [x] 6.1 复用签发逻辑，实现选择私钥、路径记忆、机器码输入、生成及复制；私钥不进入渲染层或工具持久化目录
- [x] 6.2 测试取消选择、无效/丢失私钥、焦点恢复、签名正确性和重启路径记忆
- [x] 6.3 打包独立 Windows x64 便携 exe、win-unpacked 和 zip；验证实际打包程序并扫描产物无私钥
- [x] 6.4 更新中文厂家使用说明和验收结果，不将工具交付等同于会员管理端剩余验收完成
