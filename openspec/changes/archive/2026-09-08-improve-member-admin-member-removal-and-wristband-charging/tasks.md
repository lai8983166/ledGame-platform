## 1. 先建立失败测试与契约

- [x] 1.1 为会员软删除成功、取消、关联 `READY`/`ACTIVE` 手环阻止、运行中游戏阻止、历史保留及同手机号重新注册增加真实 SQLite 集成测试，并先验证当前缺少删除接口时失败
- [x] 1.2 为共享 API 客户端增加按会员 ID 删除的请求与稳定错误契约测试，并先验证当前客户端缺少该方法
- [x] 1.3 为会员详情危险操作、确认弹层、取消、成功刷新和冲突错误展示增加管理端测试，并先验证当前 UI 没有删除入口
- [x] 1.4 为充值会话的 `idle/scanning/checking/details/submitting` 状态、数字加回车、重复提交、取消、迟到响应、分钟校验及失败重试增加前端状态测试，并先验证当前常驻输入交互不能通过
- [x] 1.5 为充值页面稳定选择器增加结构测试，要求初始只有“手环充值”按钮且 UID 不可手输，并把新增测试纳入 `pnpm test:client` 或 `pnpm test:desktop`

## 2. 实现会员软删除服务

- [x] 2.1 为 `members` 增加幂等 `deleted_at` 迁移并更新新建数据库 schema，通过旧库迁移与重复启动测试
- [x] 2.2 新增事务型 `DELETE /api/members/{id}`，校验活动会员并写入软删除标记，通过删除成功、重复删除和不存在会员测试
- [x] 2.3 在删除事务中检查开放手环绑定和运行中游戏，返回稳定冲突码且不改变任何状态，通过两类阻止场景的集成测试
- [x] 2.4 收紧会员列表、创建、Player Info、排行榜、手环绑定和游戏准入的活动会员谓词，通过已删除会员不可查询、不可准入且不参与排名的测试
- [x] 2.5 保留旧会员绑定和游戏记录，并允许管理端与自助端使用同手机号创建新 ID、零积分会员，通过完整重复注册集成测试

## 3. 接入会员管理端删除交互

- [x] 3.1 在共享客户端中实现类型化删除会员调用并通过客户端契约与 `pnpm typecheck:packages`
- [x] 3.2 在会员详情抽屉底部增加危险操作区和应用内确认弹层，展示姓名、手机号、可重新注册及保留历史说明，通过结构与取消测试
- [x] 3.3 接入删除请求，成功后关闭详情并刷新真实会员列表，冲突时保留上下文并展示服务端原因，通过状态测试
- [x] 3.4 为新增操作补充稳定测试选择器、必要文案与样式，并通过 `pnpm typecheck:member-admin` 和 `pnpm build:member-admin`

## 4. 实现按钮触发的手环充值会话

- [x] 4.1 提取纯充值会话状态机，实现会话版本、UID 缓冲、仅扫描态键盘捕获、取消清理和迟到响应隔离，通过状态单元测试
- [x] 4.2 用“手环充值”按钮替换常驻 UID/分钟输入区，点击后打开可取消的“请刷手环”弹层，并通过初始状态、打开、取消和非扫描输入测试
- [x] 4.3 扫描数字 UID 和回车后只读检查手环，接受未知或 `IN_STOCK` UID，拒绝其他状态并允许重试，通过扫描资格和重复 Enter 测试
- [x] 4.4 扫描成功后展示只读 UID、1–1440 分钟输入框及 `[30,45,60,90,120]` 快捷选项，取消时不调用写接口，通过分钟选择与无副作用取消测试
- [x] 4.5 仅在显式确认时调用充值接口，提交期间禁用重复操作，成功关闭弹层并刷新列表，失败保留 UID/分钟数供重试，通过异步提交测试
- [x] 4.6 清理离开页面和组件卸载时的键盘监听与会话数据，补充弹层样式和稳定选择器，并通过管理端类型检查、构建及客户端测试

## 5. 核心流程回归与验收

- [x] 5.1 更新管理端 Playwright 验收，覆盖点击充值、刷测试 UID `2283055618`、选择分钟、确认成功以及两个阶段取消无写入
- [x] 5.2 增加“管理端注册—删除—自助端同手机号重新注册”和反向入口的核心流程验收，确认新会员 ID、零积分及旧历史隔离
- [x] 5.3 运行 `pnpm test:core`、`pnpm test:desktop`、`pnpm typecheck` 和 `pnpm build`，修复全部回归
- [x] 5.4 运行相关 Playwright 桌面验收与 `pnpm test:e2e`，确认未删除会员的充值、绑定、游戏结算、Player Info 和排行榜流程不变
- [x] 5.5 执行 `openspec validate improve-member-admin-member-removal-and-wristband-charging --strict`，确认规格和任务可进入实施

## 6. 接入真实运营总览与紧凑手环工作台

- [x] 6.1 为充值流水、门店时区日界线、活动会员统计、成功/失败充值和今日每分钟一元收入增加真实 SQLite 集成测试，并先验证当前缺少总览接口和充值流水时失败
- [x] 6.2 为管理端总览真实 API、真实房间、错误/空状态、手动刷新和移除设备健康增加结构测试；为手环页紧凑 Grid 与响应式回落增加布局测试，并先验证现有演示 UI 失败
- [x] 6.3 新增充值流水 schema，确保每次成功充值在原事务中写入固定单价与金额；实现 `GET /api/dashboard/overview` 并通过服务端集成测试
- [x] 6.4 在共享 API 客户端接入类型化总览接口，重写运营总览以并行读取真实统计和真实房间，删除设备健康 Card，补充手动刷新、加载、错误和空状态
- [x] 6.5 将手环页头、充值、清除余额、流程提示与回收操作重排为紧凑响应式 Grid，保持状态列表全宽及所有既有操作、选择器和充值弹层行为不变
- [x] 6.6 更新相关验收选择器与断言，运行 `pnpm test:core`、`pnpm test:desktop`、`pnpm typecheck`、`pnpm build`、相关 Playwright 和 OpenSpec strict validation
