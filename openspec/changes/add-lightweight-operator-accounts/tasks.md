## 1. SQLite 账号模型与登录 API

- [x] 1.1 先增加 Platform schema/migration 集成测试，覆盖新库建立且仅建立一个可配置 `FACTORY_ADMIN`、旧库幂等升级、后续启动不覆盖已有账号、用户名大小写不敏感唯一，并确认新增断言在实现前失败
- [x] 1.2 增加 `spring-security-crypto` 依赖、账号与操作日志表、出厂账号配置和幂等初始化逻辑，运行 schema/migration 定向测试并检查数据库中只保存 BCrypt 摘要而非 `888888` 明文
- [x] 1.3 先增加登录 API 集成测试，覆盖正确密码、错误密码、未知账号、停用账号、响应不泄露 `passwordHash` 及出厂配置覆盖，并确认新增断言在实现前失败
- [x] 1.4 实现账号查询/密码校验服务与一次性登录 API，使用统一稳定错误响应，运行登录 API 定向测试验证通过
- [x] 1.5 先增加账号管理 API 集成测试，覆盖列表、创建、修改、重设密码、启停、用户名冲突及出厂管理员不可停用/改类型，并确认新增断言在实现前失败
- [x] 1.6 实现仅包含 `FACTORY_ADMIN` 与 `OPERATOR` 的账号管理 API，不增加删除、会话或后端调用者角色判断，运行账号管理定向测试验证行为边界
- [x] 1.7 运行 `pnpm test:server`，确认会员、手环、房间、排行榜、SQLite 迁移和核心流程现有后端测试没有回归

## 2. 前端内存账号状态与登录门禁

- [x] 2.1 先扩展共享 API 客户端测试与类型，覆盖登录、公开账号资料、账号列表和账号管理请求/错误，并确认实现前新增客户端断言失败
- [x] 2.2 实现共享操作账号类型和 API 客户端方法，运行 `packages/api-client/src/index.test.ts` 并执行 `pnpm typecheck:packages`
- [x] 2.3 先为无持久化 `operatorSession` 和固定角色策略增加单元测试，覆盖登录赋值、退出清空、重新创建状态为空、`FACTORY_ADMIN` 全功能及 `OPERATOR` 日常功能/危险功能区分，并确认实现前失败
- [x] 2.4 实现内存 `operatorSession` 与纯函数角色策略，明确不访问 `localStorage`、Cookie 或 Electron 配置，并运行对应单元测试
- [x] 2.5 先增加登录页面和 App 门禁结构测试，覆盖未登录只渲染登录、登录失败停留、登录成功进入管理框架、侧边栏显示真实账号、退出回到登录页和无找回密码入口，并确认实现前失败
- [x] 2.6 实现登录页面、App 登录门禁、真实账号资料与退出按钮，接入现有 i18n 工作流并运行登录/UI 定向测试和 `pnpm i18n:check`

## 3. 账号管理界面与轻量 UI 权限

- [x] 3.1 先增加普通操作员 UI 权限结构测试，覆盖隐藏系统设置导航、会员删除、主动清空手环余额、房间改名和账号管理，同时保留会员注册、手环充值/回收、房间查看、排行榜和记录入口，并确认实现前失败
- [x] 3.2 将统一角色策略接入 App、会员、手环、房间和设置页面，避免页面直接比较用户名，运行权限结构测试确认两类账号界面差异
- [x] 3.3 先增加账号管理状态与组件测试，覆盖列表加载、创建、编辑、重设密码、启停普通账号、用户名冲突、提交防重复及出厂管理员只读保护，并确认实现前失败
- [x] 3.4 在系统设置页实现操作账号管理区域及对应状态模块，只向出厂管理员展示，不提供账号删除和出厂账号停用/改类型控件，运行账号管理 UI 测试
- [x] 3.5 增加切换账号/退出后的页面复位测试，确认普通操作员不能通过之前打开的抽屉、弹窗或当前页面残留继续看到出厂管理员入口
- [x] 3.6 运行 `pnpm test:client`、`pnpm typecheck:member-admin` 和 `pnpm build:member-admin`，确认现有会员管理端交互、筛选、弹窗焦点和多语言基线没有回归

## 4. 操作账号上下文与关键业务留痕

- [x] 4.1 先扩展共享 API 客户端和 Electron `api-transport` 测试，覆盖内存账号提供器自动添加 `X-Operator-Id`、未登录不添加、只白名单转发该头且不开放任意请求头，并确认实现前失败
- [x] 4.2 实现操作账号请求头注入与桌面传输白名单，运行 API 客户端和 `desktop/tests/shared-runtime.test.mjs`、`member-admin-runtime.test.mjs` 验证浏览器/桌面请求一致
- [x] 4.3 先增加后端操作账号解析与日志服务集成测试，覆盖存在账号、未知账号稳定错误、账号名称快照、摘要不含密码及业务失败不产生成功日志，并确认实现前失败
- [x] 4.4 实现统一操作账号解析器和事务内 `operator_action_logs` 写入服务，运行定向测试验证账号 ID 与名称快照正确
- [x] 4.5 先为账号创建/修改、会员注册/删除、手环充值/清空/解绑/回收、房间改名和持久化系统设置增加操作日志集成断言，并确认尚未接线的操作在实现前失败
- [x] 4.6 将统一操作日志接入现有关键管理写操作，管理端会员创建的 `createdBy` 由已解析账号派生，运行相关会员、手环、房间及账号管理集成测试
- [x] 4.7 运行自助注册端和游戏端核心接口回归测试，确认没有 `X-Operator-Id` 时原有注册、绑定、游戏准入、房间连接和结算仍然通过

## 5. 桌面验收、文档与最终校验

- [x] 5.1 先扩展会员管理 Electron 测试，覆盖启动显示登录页、使用测试出厂账号登录、退出后回到登录页、应用重启不恢复账号，并确认新增断言在实现前失败
- [x] 5.2 更新开发桌面、打包运行与验收 profile 的出厂账号配置，完成 Electron 登录流程并运行 `pnpm test:desktop:member`
- [x] 5.3 更新门店 acceptance harness，集中提供测试出厂账号登录 helper，使涉及会员管理端的既有大型场景先登录后继续，并增加出厂管理员/普通操作员界面与操作留痕验收场景
- [x] 5.4 运行 `pnpm test:acceptance:unit` 和相关 Playwright 门店验收，确认账号机制不影响自助注册端、游戏端、多玩家和结算交付核心流程
- [x] 5.5 更新中文开发/打包与门店使用说明，写明默认 `admin / 888888`、厂家可配置覆盖、无找回入口、普通覆盖安装会保留 SQLite，以及恢复出厂可能造成的数据影响
- [x] 5.6 运行 `pnpm test:core`、`pnpm test:desktop`、`pnpm typecheck`、`pnpm build` 和 `openspec validate add-lightweight-operator-accounts --strict`，汇总测试结果并确认全部任务勾选后再进入归档
