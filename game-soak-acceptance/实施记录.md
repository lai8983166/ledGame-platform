# 实施记录

主 Change：`add-packaged-game-soak-acceptance`。默认目标 18 小时，仅游戏端及自有后端。

## 已核对入口

| 仓库 | 位置 | 用途 |
| --- | --- | --- |
| ledGame | electron/main.cjs | LED_USER_DATA_DIR 隔离；打包版启动自有 Java；后端端口、房间连接配置 |
| ledGame | src/views/LedGameTouchView.vue | players → game → level → 5 秒倒计时；STOPPED 手动返回待机；每步 20 秒超时 |
| ledGame | src/lib/gameCatalog.js | 玩家列表过滤；UI 初始化会尝试 seed，必须在隔离副本中运行 |
| ledGame-backend | service/PlayableGameCatalogService.java | GET /games/playable，只读实际可见 ID |
| ledGame-backend | service/GameRuntimeStateService.java | GET /engine/game/state，追加 sessionId 和 runningMillis |
| ledGame-backend | GameApp/Runtime/GameTermination.java | NATURAL_SUCCESS / NATURAL_FAILURE / MANUAL_STOP |
| ledGame-backend | GameApp/Runtime/rank/RankRuntimeAssembler.java | Rank 使用首个玩法配置 durationSeconds |
| ledGame-backend | service/GameHardwareReadinessConfiguration.java | acceptance 显式就绪替身，普通启动不放开 |
| ledGame-backend | hardware/elc408/transport/Elc408UdpTransport.java | 实际 UDP 收发边界，新增独立计数 |
| ledGame-platform | acceptance/support/bidirectionalFloorDevice.ts | 历史最多 50 条，但 validFrameCount 是样本条数，不能直接作为烤机累计计数 |

游戏前端配套 Change：`add-packaged-game-soak-support`。
游戏后端配套 Change：`add-game-soak-observability`。

## 与内存审计兼容

2026-09-06 继续实施时，保留 MemberGameAccessCoordinator 和 GameQueueService 的审计改动及测试。先运行这两类测试：27 项通过。未回退审计，也未修改它们。

## 当前验证

- 平台：`node --test game-soak-acceptance/tests/*.test.mjs`，43 项通过（包含 Windows 进程身份清理核验）。虚拟时钟不是实际 18 小时。
- 前端：`node --test tests/game-soak-support.test.mjs`，2 项通过；`pnpm build` 通过。
- 后端：11 个聚焦测试类，81 项通过，覆盖 SDK、注入、生命周期、普通硬件就绪约束及会员/排队内存审计回归。命令见下。

```text
mvn -q -Dtest=SoakInputServiceTest,SoakInputControllerTest,GameHardwareReadinessConfigurationTest,Elc408SdkServiceTest,Elc408MetricsTest,GameRuntimeInputDispatchTest,GameFloorInputEquivalenceTest,GameEnginePreparationLifecycleTest,GameSoakEvidenceTest,MemberGameAccessCoordinatorTest,GameQueueServiceTest test
```

## 旧 SDK 回归问题（已对齐测试）

2026-09-06 经用户确认，以 2026-08-19 实机调试提交 `6441213` 为依据更新以下旧断言，不修改 SDK 发送实现。两个异步调试用例等待首帧完成后检查报文；测试采集列表改为线程安全快照，并保证断言失败也关闭服务。新增单/双控制器连续两帧测试，验证首帧 FFF0 位置及 RGB 编号、后续帧不重发 FFF0 且编号重置。

`mvn -q -Dtest=Elc408SdkServiceTest,Elc408MetricsTest test` 连续运行 3 次，每次 27 项通过（SDK 25、计数 2）。此前的旧测试阻碍已解除，但不代表完整烤机或真实硬件 18 小时验收通过。以下为修复前的排查记录。

`Elc408SdkServiceTest` 全类执行出现 5 项失败。使用独立 worktree 在未包含本次改动及内存审计的 `f0dd526` 基线上复现其中 4 项：

- sendsLegacyControllerIdsForEveryDisplayPlan
- startsMultiControllerDebugAfterMatchingSearch
- pointTestUsesTheMappedControllerInMultiControllerWiring
- sendsFff0ForEveryControllerWithPrefixedProtocolTypes

另 1 项 multiControllerDebugAllowsSingleSearchResultAndSendsAllControllerFrames 最初单独重跑通过，随后和其他异步帧测试一起补充等待及线程安全快照。连续三次回归已通过。

最终按用户确认修改旧断言，没有改 SDK 报文生成规则来凑测试通过。新增计数只接入实际收发、解析和 socket 生命周期边界；收发超时/异常及实际解析拒绝已有故障测试。socket 创建并不证明控制器物理在线。

## 打包 UI 实测证据（2026-09-06）

- 测试游戏包：`F:/project/ledGame/.build/game-soak-package/win-unpacked/LED Game.exe`，没有覆盖原 release 游戏包。
- 无注入 Rank 短测：`.build/game-soak-runs/soak-1788679566392/测试报告.md`，便携工具自带运行时启动，2 局自然结束，源文件哈希复核通过；结束后按采样 PID 核查没有留下本轮进程。
- 带注入 Rank 短测：`.build/game-soak-runs/soak-1788696174856/测试报告.md`，2 局、4 个模拟 DOWN/UP 事件、0 拒绝；没有真实 SDK 接收样本，真实通信明确未验证。
- 四玩法短测：`.build/game-soak-runs/soak-1788696508106/测试报告.md`，simple → normal → diffcult → Rank，完整循环两遍，8 局；总时间 366.904 秒，累计 RUNNING 150.103 秒；5,712 帧运行中模拟输出、0 拒绝帧；40 次内存采样、0 缺口。
- 最终便携回归：`.build/game-soak-runs/soak-1788697153283/测试报告.md`，重新打包的最新游戏端，2 局自然结束，108.862 秒，2,292 帧运行中输出、0 拒绝；14 次采样、0 缺口；exe/asar/jar 与源库哈希复核、PID+创建时间退出核验均通过。
- 四玩法使用显式开发夹具：`tests/create-short-fixture.mjs` 只在独立副本将前三种玩法设为 15 秒，Rank 保留 30 秒。第一次夹具关闭过早，最后一次保存未出现在重启后的副本中，预检以 1,220 秒预算拒绝开测；增加 H2 延迟写入等待后重新生成副本，四玩法短测通过。正式 runner 不会缩短游戏时间，该夹具生成脚本不打入便携工具。
- 普通模式接口默认不存在；启用后只允许本机、runId/当前 RUNNING 会话、递增序号和每秒最多 200 个 DOWN/UP。真实与模拟按住独立合并，同坐标释放、过期会话和结束清理已测试。
- 输出每 10 秒采样，保留单张失败截图；本轮目录超过 512MB/10,000 项或输出盘不足 100MB 时明确停止并保留现场，不删除日志掩盖溢出。

## 交付和未验证范围

`pnpm portable:game-soak` 生成独立便携目录，包含 Node、Playwright、中文说明及空游戏列表模板，不包含业务库、测试夹具或厂家密钥。命令为 `soak.cmd inspect config.json` / `soak.cmd run config.json`，只启动游戏端。

本次最新便携目录：`release/game-soak-tool-1788697309880`。和最终回归使用的 `game-soak-tool-1788697042894` 比较，运行模块相同，补更新中文说明和构建交付内容检查；依赖自检通过。三个配套 Change 的 `openspec validate <change> --strict` 均通过。主任务为 20/21，剩余实际现场 18 小时。

短测均为 `game` / `touch` / `PRODUCTION` 生命周期和 `simulated` 输出。未接真实地砖，不支持据此宣称网络、物理控制器、实际 8×8 地砖或 18 小时通过。短于内存趋势窗口且没有预声明阈值，内存结论为“未判定”。真实 18 小时任务保持未完成。
