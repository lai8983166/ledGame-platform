## 1. 控制协议与 A 机入口

- [x] 1.1 定义当前批次、代理状态和本地加入配置的数据契约，并以配置解析测试验证非法端口、URL、档位和 agentId 会在运行前被拒绝
- [x] 1.2 实现 A 机轻量 HTTP 控制器、唯一 runId 和现有中心启动组合，并以单元测试验证健康检查、当前批次及预检失败行为
- [x] 1.3 实现 B/C 就绪屏障、运行状态与离线提示，并以控制器测试验证单节点等待、双节点共同开始和跨批次拒绝

## 2. B/C 一键加入与证据回传

- [x] 2.1 实现代理本地配置的首次保存、无参数读取和等待 A 机逻辑，并以测试验证配置复用、暂时不可达及无效响应不会触发负载
- [x] 2.2 将远程批次参数接入现有代理执行函数，发送节流进度且保持业务写请求不重试，并以集成测试验证 B/C 仍生成确定性本地证据
- [x] 2.3 实现固定证据的流式接收、大小与 SHA-256 校验、原子落盘及同内容幂等上传，并以测试验证目录穿越、冲突覆盖、哈希错误和重传

## 3. 自动核账与操作入口

- [x] 3.1 在 B/C 证据齐全后复用现有核账器执行一次自动核账、输出最终状态并尝试打开中文报告，以集成测试验证与手工 verify 结果一致
- [x] 3.2 增加 A 机启动和 B/C 加入命令及 Windows 双击入口，更新便携构建并检查新旧命令、示例配置和必要运行文件均进入产物
- [x] 3.3 将中文说明收敛为推荐三步流程、首次配置、实时状态和失败兜底，并复核不再要求常规复制 connection 或 B/C 结果目录

## 4. 总体验证

- [x] 4.1 补充本机双代理端到端自动化，验证自动生成批次、双机屏障、负载执行、证据回传和自动报告全链路通过
- [x] 4.2 运行 multipoint-concurrency 全部测试、TypeScript 构建、便携打包和 OpenSpec 严格校验，并记录尚需三台真实机器 quick 复验的边界
