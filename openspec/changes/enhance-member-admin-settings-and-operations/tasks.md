## 1. 数据库与服务端基础

- [x] 1.1 为 `store_settings` 建立幂等迁移和单例初始化，补齐应用标题、图标资源引用/哈希、每分钟价格、副屏开关及更新时间，并验证旧数据库迁移后仍保留原有会员、交易和游玩数据
- [x] 1.2 实现品牌图标资源的受控存储、格式/尺寸/文件大小校验、哈希校验和原子替换，并验证资源缺失或损坏时返回默认图标
- [x] 1.3 增加门店设置查询与按字段更新 API，复用现有 operator 权限、输入校验和审计日志，验证未授权请求被拒绝且合法修改可在重启后读取
- [x] 1.4 将充值服务改为在事务中读取持久化的每分钟价格并计算金额，保留整数分精度，验证默认 100 分/分钟、自定义价格和非法价格均符合规格
- [x] 1.5 增加会员资料更新 API，限制可编辑字段，复用加密字段与检索哈希更新策略，验证手机号唯一性、格式校验、事务回滚和会员 ID/历史字段不可修改
- [x] 1.6 扩展发卡/充值记录迁移，增加操作员 ID、用户名/显示名快照和独立 `issued_at`，使新写入记录强制带操作员上下文，并验证历史记录以“历史记录/未知”兼容展示
- [x] 1.7 扩展发卡记录、充值记录查询与导出映射，返回操作员、发卡时间、充值时间、单价和金额，并验证日期筛选和旧记录导出不丢字段
- [x] 1.8 扩展房间状态接收与规范化逻辑，从游戏端可选的 `gameplay.players` 投影玩家会员、手环、当前积分和参与状态，在服务端计算稳定并列排名，并验证旧客户端缺失该字段时仍可正常连接
- [x] 1.9 提供副屏所需的日榜、月榜、年榜聚合读取能力，复用现有门店时区和已结算积分逻辑，验证 RUNNING/放弃记录不会进入榜单、无数据和部分失败有明确结果

## 2. 共享 API 客户端与类型

- [x] 2.1 更新共享 API 类型，覆盖门店设置、品牌图标、充值规则、会员更新结果、发卡审计字段、房间实时玩家和三周期排行榜
- [x] 2.2 增加设置读取/更新、会员更新、三榜聚合和审计记录查询客户端方法，验证请求路径、权限头、错误码和默认值转换与服务端契约一致
- [x] 2.3 更新房间状态映射，将服务端实时玩家、积分和排名映射到现有 `Room.players`，验证空闲、断线和过期状态不会残留上一局玩家数据

## 3. 会员管理端界面

- [x] 3.1 在配置页加入应用标题、运行时图标、每分钟收款金额和副屏开关表单，显示当前生效规则和默认值，验证保存、取消、非法输入提示与权限禁用
- [x] 3.2 在房间管理卡片和详情中展示当前游戏、参与会员、实时积分与排名，并显示空闲、离线、数据过期提示；验证收到连续状态更新时不重复累加积分
- [x] 3.3 在会员详情加入编辑/取消/保存流程，支持姓名、手机号、性别、生日和头像等可编辑字段，验证保存成功回填列表与详情、重复手机号显示后端错误
- [x] 3.4 更新发卡记录与交易记录列表，展示操作员、发卡时间、充值时间、充值分钟、单价和金额，验证历史记录的未知字段不会伪造当前操作员
- [x] 3.5 增加副屏设置后的状态反馈、加载/失败/重试提示，不阻塞主窗口；验证关闭副屏后主界面和其他业务仍可使用

## 4. 会员管理端桌面能力

- [x] 4.1 增加品牌设置 IPC 与主进程应用逻辑，启动时先使用默认品牌、后异步读取持久化品牌，保存后即时更新窗口标题和任务栏/窗口图标，并验证重启恢复
- [x] 4.2 实现副屏窗口的创建、显示、隐藏和销毁生命周期，确保副屏异常不阻塞主窗口，并验证设置开关重启后保持状态
- [x] 4.3 实现副屏三栏（日榜/月榜/年榜）渲染和定时刷新，显示数据时间、空榜和请求失败提示，验证网络恢复后立即刷新且不把旧数据标成实时
- [x] 4.4 将品牌图标资源纳入现有备份资产清单或提供缺失回退路径，验证备份恢复后设置引用不会指向失效文件

## 5. 自动化与集成验收

- [x] 5.1 补充服务端迁移、设置、价格计算、会员更新、发卡审计和权限测试，并运行会员管理端服务测试套件
- [x] 5.2 补充房间 WebSocket 投影测试，覆盖多玩家积分更新、并列排名、乱序/过期事件、断线和旧协议兼容
- [x] 5.3 补充会员管理端前端测试，覆盖设置表单、会员编辑、房间实时数据、发卡字段和副屏三榜渲染
- [x] 5.4 补充桌面端 IPC/窗口集成测试，覆盖默认品牌降级、持久化品牌、图标缺失、副屏开关和网络失败不阻塞主窗
- [x] 5.5 执行跨端核心流程回归：充值使用新价格、发卡记录绑定操作员、游戏端上报实时积分、结算后日月年榜更新，并记录可复现的验收结果
