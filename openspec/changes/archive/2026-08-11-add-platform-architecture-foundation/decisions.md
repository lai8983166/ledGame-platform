# 平台决策清单 · 待决策项

> **用途**：本文件仅保留尚未确认（⚠️ 倾向 或 ❓ 待定）的决策项。
> 已锁定的决策见 `decided.md`。两个文件互斥，便于评审会聚焦未决项。
>
> **维护规则**：随时可补充新决策；新增用下一个可用 ID。一旦确认即迁入 `decided.md` 并从本文件剔除。

---

## 图例

| 标记 | 含义 |
|---|---|
| **优先级** | |
| 🔴 甲方决策 | 业务模式、成本、合规、用户体验权衡。**必须甲方拍板** |
| 🟡 技术决策 | 架构选型、技术方案。技术负责人决定，但需告知甲方 |
| 🟢 默认采用 | 业内标准最佳实践，无需打扰甲方 |
| **状态** | |
| ⚠️ 倾向 | 有明确推荐方向，待最终确认 |
| ❓ 待定 | 完全开放，需要讨论 |

> **环境背景**（已定，影响下方多个决策）：部署在 **Windows** 上（A4）。涉及进程自愈、脚本、定时任务、远程接入等都要按 Windows 生态考虑。

---

## A. 部署与基础设施

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| A5 | 容器化 vs 裸部署 | 🟡 | ❓ | A. Docker Desktop / WSL2（Windows 上略重）<br>B. Windows Service + jar 直跑（推荐）<br>C. NSSM 包装 jar | 部署一致性、运维复杂度 |

---

## B. 多端通信架构

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| B6 | 时区策略 | 🟡 | ❓ | A. 全部存 UTC，前端转本地（推荐）<br>B. 全部存本地时区<br>C. 混用 | 跨时区扩展、调试便利性 |
| B7 | 时间一致性（NTP） | 🔴 | ❓ | 是否强制所有设备 NTP 同步？手环计时对时钟敏感 | 计时准确性、设备配置规范 |
| B8 | 桌面端断网行为 | 🔴 | ❓ | A. 立即终止游戏（保护商家）<br>B. 允许继续 X 分钟后强制结束（推荐）<br>C. 允许继续直到自然结束 | 顾客体验 vs 商家风险 |
| B9 | 设备掉线恢复 | 🟡 | ❓ | 桌面端重启/断网恢复后，如何拉取错过的事件？增量游标 / 全量对账 | 数据一致性、实现复杂度 |
| B11 | 设备注册/绑定机制 | 🔴 | ❓ | A. 预置令牌（部署前 admin 配进设备，推荐）<br>B. 首次启动配对（admin 在管理端审批）<br>C. 物理按钮+一次性激活码 | 部署便利性、安全性 |
| B14 | 请求超时与重试策略 | 🟡 | ❓ | 各端调用 API 的默认超时、失败重试次数、退避算法 | 用户体验、服务器压力 |

---

## C. 认证授权

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| C2 | 角色体系 | 🔴 | ❓ | A. 单一管理员（所有人权限相同）<br>B. 店长+员工两级（推荐）<br>C. 多级 RBAC（店长/主管/员工/财务） | 业务流程、实现复杂度 |
| C3 | 管理员身份方式 | 🟡 | ⚠️ | **倾向**：账号密码登录；密码 bcrypt 哈希；登录后发 session cookie 或 JWT | 安全、可审计 |
| C4 | Session vs JWT | 🟡 | ❓ | A. Session（server 端存储，便于踢人，推荐）<br>B. JWT（无状态，但难主动失效） | 复杂度、安全性 |
| C5 | kiosk 设备认证 | 🟡 | ⚠️ | **倾向**：设备令牌（admin 在管理端为每台 kiosk 生成唯一 token，预置到设备配置） | 防止 API 被脚本绕过 |
| C6 | 游戏桌面端认证 | 🟡 | ⚠️ | **倾向**：同 C5（设备令牌） | 同 C5 |
| C7 | 多端登录限制 | 🔴 | ❓ | 同一管理员账号能否同时多端登录？A. 允许<br>B. 互踢<br>C. 禁止 | 用户体验、安全 |
| C8 | 密码策略 | 🔴 | ❓ | A. 仅长度 ≥ 8<br>B. 长度+复杂度<br>C. 强度+定期更换 | 安全、易用性 |
| C9 | 密码找回 | 🔴 | ❓ | A. 店长重置员工密码（推荐）<br>B. 邮件/短信自助找回<br>C. 不做（联系供应商） | 客服流程、依赖外部服务 |
| C10 | 操作来源审计粒度 | 🟡 | ⚠️ | **倾向**：所有变更类操作记录 `who + when + what + from_device` | 审计能力、存储成本 |

---

## E. 跨模块业务模型（全部待定，影响最广）

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| E1 | 会员等级体系 | 🔴 | ❓ | A. 不分等级（推荐 MVP）<br>B. 普通/银/金/钻<br>C. 按消费自动升级 | 营销能力、实现复杂度 |
| E2 | 会员卡类型 | 🔴 | ❓ | A. 永久会员（推荐 MVP）<br>B. 月卡/季卡/年卡<br>C. 储值卡 + 时长卡分离 | 商业模式、计费逻辑 |
| E3 | 积分能否换购/抵现 | 🔴 | ❓ | A. 积分仅做排行（推荐 MVP）<br>B. 积分可抵现<br>C. 积分可换购商品 | 营销、财务复杂度 |
| E4 | 团体/包场 | 🔴 | ❓ | A. 不支持（推荐 MVP）<br>B. 支持（团购价、单独结算） | 业务流程、计费 |
| E5 | 营销活动 | 🔴 | ❓ | A. MVP 不做<br>B. 简单赠送时长<br>C. 完整促销引擎 | 营销能力、复杂度 |
| E6 | 跨门店积分/会员 | 🔴 | ✅ | （已定，见 `decided.md`：A1 决定不跨门店。**此项可删**，保留只是为了提醒未来扩展时再讨论） | 商业扩展性 |
| E7 | 是否支持退款 | 🔴 | ❓ | A. 不退款（仅消费）<br>B. 当日可退<br>C. 任意可退（需财务审批） | 财务流程、客户体验 |
| E8 | 发票 | 🔴 | ❓ | A. 不支持<br>B. 手动开发票<br>C. 电子发票（接第三方） | 合规、外部依赖 |
| E9 | 现金/扫码 收款对账 | 🔴 | ❓ | 系统只记账，实际收款如何对账？每日人工 / 系统对接扫码流水 | 财务流程、防漏款 |
| E10 | 客户自助查询 | 🔴 | ❓ | 是否需要小程序/公众号让顾客查自己积分、剩余时长？ | 客户体验、开发成本 |

> 注：账户模块整体已移出 MVP（见 `decided.md` G 组），所以 E1-E5 涉及的"余额/积分/卡类型"等实质功能在 MVP 阶段都暂不落地。这些决策可以延后到"上线后第二阶段"再讨论。

---

## F. 会员 + 手环模块 · 遗留小问题

> 详见 `openspec/changes/add-member-wristband-backend/design.md`。无需甲方介入，实现阶段确认即可。

| ID | 问题 | 推荐 |
|---|---|---|
| F-open-1 | FROZEN 会员的现有 ACTIVE 绑定是否立即失效 | 默认保留至到期 |
| F-open-2 | 过期扫描走懒触发还是定时任务 | 懒触发 |
| F-open-3 | 事件发布在事务内还是事务后 | 事务后 |
| F-open-4 | 持久层选型 | **已定：MyBatis**（见 decided.md D5） |

---

## G. 账户模块（仅保留流水/审计相关）

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| G7 | 流水记录字段 | 🟡 | ⚠️ | **倾向**：谁/何时/金额/方向/关联业务/支付方式/操作员 | 审计能力 |
| G8 | 余额变更审计 | 🟡 | ⚠️ | **倾向**：所有变更记前后值 + 操作来源 | 客诉处理、纠纷 |

> 账户整体不在 MVP。这两项留作"将来讨论流水/审计粒度"的占位。

---

## H. 房间/设备（仅剩硬件状态字段）

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| H6 | 设备硬件状态字段 | 🟡 | ❓ | 在线/离线/异常；故障类型细分到什么程度？ | 故障排查 |

---

## I. 游戏模块（仅剩配置归属）

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| I2 | 游戏规则配置由谁维护 | 🔴 | ❓ | A. 代码写死（不推荐）<br>B. admin UI 可配置<br>C. 配置文件<br>D. **桌面端自己管**（与 I5 一致，推荐） | 运营灵活性 |
| I6 | 游戏素材资源管理 | 🟡 | ❓ | A. 游戏侧自带，中心不存（与 I5 一致，推荐）<br>B. 中心统一管理 + 同步到桌面端 | 一致性、复杂度 |

---

## J. 记录/审计（仅剩权限）

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| J5 | 审计查询权限 | 🔴 | ❓ | A. 所有管理员可见<br>B. 仅店长可见（推荐）<br>C. 区域经理/总部可见（多门店扩展） | 隐私、管理 |

---

## K. 同步模块

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| K1 | 同步事件投递保证 | 🟡 | ⚠️ | **倾向**：至少一次（at-least-once）+ 业务幂等。不做 exactly-once | 一致性、复杂度 |
| K2 | 消息表（Outbox）结构 | 🟡 | ⚠️ | **倾向**：`id / aggregate_type / aggregate_id / event_type / payload / created_at / published_at / consumer_cursor` | — |
| K3 | 重试策略 | 🟡 | ❓ | 间隔：1s/5s/30s/5min/30min/2h/24h。最多几次后进死信？ | 可用性、噪音 |
| K4 | 死信处理 | 🟡 | ❓ | A. 仅告警，人工处理（推荐）<br>B. 自动放弃<br>C. 备份后清理 | 数据完整性、运维 |
| K5 | 同步事件粒度 | 🟡 | ❓ | A. 字段级（细粒度，复杂）<br>B. 实体级（推荐）<br>C. 全量（简单但浪费） | 网络流量、复杂度 |
| K6 | 设备离线后的补偿窗口 | 🔴 | ❓ | 设备离线多久内的消息必须补上？超过多久直接放弃 + 全量对账？ | 一致性、存储 |
| K7 | WebSocket 断线重连 | 🟡 | ❓ | 自动重连 + 拉取断线期间事件。具体退避算法？ | 体验、复杂度 |

---

## L. 运维

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| L1 | 监控告警通知方式 | 🔴 | ❓ | A. 邮件<br>B. 钉钉/企业微信机器人（推荐）<br>C. 短信（成本）<br>D. 多种组合 | 响应速度、成本 |
| L2 | 关键监控指标清单 | 🟡 | ❓ | 至少：服务存活、DB 连接、磁盘使用、错误率、关键业务（发卡成功率）。其他？ | — |
| L3 | 告警阈值 | 🟡 | ❓ | 磁盘 80%/90%；错误率 > 5%；响应时间 > 2s。具体阈值？ | 噪音 vs 漏报 |
| L4 | 日志保留期限 | 🟡 | ❓ | A. 7 天<br>B. 30 天（推荐）<br>C. 90 天 | 存储、排查能力 |
| L6 | 首日部署/初始化流程 | 🔴 | ❓ | 新门店：怎么装系统（Windows）？初始管理员账号怎么设？初始手环库存怎么导入？ | 上线流程 |
| L7 | 紧急停业/维护窗口 | 🔴 | ❓ | 是否需要"暂停营业"模式（admin 一键暂停所有交易）？ | 应急能力 |
| L8 | 软件升级流程 | 🔴 | ❓ | A. 手动 jar 替换（推荐 MVP）<br>B. Windows Installer 包<br>C. 自动升级（复杂） | 停机时间、复杂度 |
| L9 | 远程支持/运维接入 | 🔴 | ❓ | 供应商/IT 是否需要远程接入门店系统排障？通过什么方式（RDP / 向日葵 / VPN）？ | 排障效率、安全 |

---

## M. 安全/合规

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| M1 | 个人信息保护合规依据 | 🔴 | ❓ | 适用《个人信息保护法》(PIPL)，是否还有地方性法规？需要法务确认 | 合规风险 |
| M2 | 用户隐私协议 | 🔴 | ❓ | kiosk 注册时是否需要顾客勾选隐私协议？协议内容由谁起草？ | 合规必需 |
| M3 | 数据留存期限（会员资料） | 🔴 | ❓ | 注销/冻结的会员数据保留多久必须删除？与 J2 审计保留期可能冲突 | 合规、存储 |
| M4 | 传输加密 | 🟡 | ⚠️ | **倾向**：TLS 必须开启（即使局域网）。自签证书还是内部 CA？ | 安全、部署 |
| M5 | 敏感数据存储加密 | 🔴 | ❓ | 手机号、生日等是否需要字段级加密？A. 不加密（与 J7 明文一致）<br>B. 应用层加密<br>C. TDE 透明加密 | 合规、性能 |
| M7 | 敏感操作二次确认 | 🔴 | ❓ | 哪些操作需要二次确认？退款 / 大额充值 / 退役手环 / 删除会员 / 修改余额 | 防误操作、防内鬼 |
| M8 | 防暴力破解 | 🟡 | ❓ | 登录失败 N 次锁定？N=5/10？锁定多久？5/15 分钟？ | 安全、易用 |
| M9 | 操作日志防删除 | 🟡 | ❓ | 审计表是否禁止任何 update/delete（仅 insert）？应用层 + DB 权限双管 | 审计可信度 |
| M10 | 数据库账号权限分离 | 🟡 | ❓ | 应用账号 vs 备份账号 vs 运维账号是否分开？最小权限原则 | 安全、运维 |

> ⚠️ **风险提示**：J7 决策为"明文"，M5 倾向"不加密"。如果 M1/M2 法务结论要求加密，**这两项需要回滚**。建议 M1/M2 优先确认。

---

## N. 客户化/扩展性

| ID | 问题 | 优先级 | 状态 | 选项 | 影响 |
|---|---|---|---|---|---|
| N1 | 多语言 | 🔴 | ❓ | A. 仅中文（推荐 MVP）<br>B. 中/英双语<br>C. 多语言 | 客户群、复杂度 |
| N2 | 多主题/皮肤 | 🔴 | ❓ | A. 单一主题（推荐 MVP）<br>B. 浅色/深色<br>C. 可定制品牌色 | UI 复杂度 |
| N3 | 自定义字段 | 🔴 | ❓ | 是否允许 admin 给会员/手环加自定义字段（如"会员备注"）？ | 灵活性、复杂度 |
| N4 | 未来多门店扩展 | 🔴 | ❓ | 当前 A1 决定单门店独立部署。未来若要多门店汇总/SaaS，预留扩展点吗？预留到什么程度？ | 架构前瞻性 |
| N5 | 开放 API 给第三方 | 🔴 | ❓ | 是否需要对外开放 API（如让客户小程序对接）？什么时候做？ | 平台化能力 |

---

## 汇总统计

| 优先级 | 总数 | ⚠️ 倾向 | ❓ 待定 |
|---|---|---|---|
| 🔴 甲方决策 | 28 | 0 | 28 |
| 🟡 技术决策 | 22 | 9 | 13 |
| 🟢 默认采用 | 0 | 0 | 0 |
| **合计** | **50** | **9** | **41** |

**与首版对比**：原 ~95 项 → 现存 50 项待决策。已锁定 59 项（见 `decided.md`）。

**甲方决策负担最重的几块**（建议下次评审会优先讨论）：
- **C 认证**：C2 角色体系、C7/C8/C9 登录密码策略
- **E 跨模块业务**：E1-E5/E7-E10（但账户整体移出 MVP，可延后）
- **B 多端通信**：B7 NTP、B8 断网行为、B11 设备注册
- **L 运维**：L1/L6/L7/L8/L9 上线和运维流程
- **M 合规**：M1/M2/M3（影响 J7/M5 是否要回滚）

---

## 待补充问题（占位）

> 讨论过程中发现的新决策点会追加在对应分类下，或临时记录在此。

- (空)

---

## 变更记录

| 日期 | 变更 | 决策者 |
|---|---|---|
| 2026-08-06 | 初版，约 95 项决策 | 设计方 |
| 2026-08-08 | 第一轮评审后剔除 59 项已定项 → `decided.md`；剩余 50 项 | 甲方/设计方 |
