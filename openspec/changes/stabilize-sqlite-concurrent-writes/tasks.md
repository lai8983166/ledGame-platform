## 1. 固化失败与连接策略测试

- [x] 1.1 先增加真实临时 SQLite 文件的数据源集成测试，读取实际 Hikari 最大池大小、`journal_mode` 和 `busy_timeout`，证明现状不满足目标后再实施配置
- [x] 1.2 先增加受控锁集成测试，用第二个 SQLite 连接分别制造短写锁和超时长写锁，验证短锁最终成功、长锁返回 503 `DATABASE_BUSY` 且没有部分写入
- [x] 1.3 增加锁异常识别单元测试，覆盖 SQLite `BUSY`、`LOCKED`、嵌套 cause 以及非锁数据库异常，验证只有前两类被识别

## 2. 实施最小生产连接策略

- [x] 2.1 将会员管理端主 SQLite 数据源配置为单池连接、WAL 和可配置的有限 `busy_timeout`，并使 1.1 的实际连接断言通过
- [x] 2.2 在现有 API 异常处理边界映射真正的 SQLite `BUSY`/`LOCKED`，验证响应为 HTTP 503、`DATABASE_BUSY` 和中文占用排查提示，其他异常仍保留原分类
- [x] 2.3 使用包含旧业务数据的数据库副本和内存测试数据库启动服务，验证配置不修改业务记录、不要求手工迁移，并且既有测试环境仍能启动

## 3. 验证业务并发与备份兼容

- [x] 3.1 先增加多线程并发充时回归测试，使用唯一 UID 同时提交至少八个请求，验证零非预期 5xx、每只手环和每条充值记录恰好写入一次
- [x] 3.2 增加真实 HTTP 混合流程集成测试，同时执行注册、绑定、激活、开局、自然结算和查询，逐项核对会员手环关联、会话唯一性、完成状态及积分
- [x] 3.3 增加在线备份与业务写入重叠测试，验证备份通过完整性检查、可读取一致状态，业务库未损坏且没有锁冲突 HTTP 500
- [x] 3.4 检查并发测试的请求耗时与事务范围；只有测试定位到具体长事务时才局部收紧该事务，并用对应回归测试证明行为不变

## 4. 全量回归与打包验收

- [x] 4.1 运行服务端完整测试以及平台 `test:core`、`test:e2e` 和桌面测试，验证现有充值、注册、游戏、账号、备份与导入行为没有回归
- [x] 4.2 重新构建会员管理端打包程序，使用隔离 userData、测试数据库和备份目录完成一次启动、关闭、备份及正式库恢复检查
- [x] 4.3 使用现有多点并发工具和两个同时运行的代理重跑原 smoke 配置，保存中文报告并确认所有计划完成、非预期 5xx、SQLite 锁证据和数据差异均为零
- [x] 4.4 运行 `openspec validate stabilize-sqlite-concurrent-writes --strict`，并核对生产改动仅位于会员管理端后端及其配置，没有加入客户端重试、自建写队列或新的数据库抽象层
