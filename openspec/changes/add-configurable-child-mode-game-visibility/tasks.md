## 1. 配置模型与兼容读取

- [x] 1.1 在游戏端现有游戏配置模型中增加 `childModeVisible` 字段，默认值为 `true`，并用旧配置读取测试验证缺少字段时仍可正常加载
- [x] 1.2 将字段纳入游戏配置保存与目录序列化契约，验证保存后重新加载得到相同值且不影响其他字段
- [x] 1.3 清理游戏端和后端基于 `simple-demo` 名称的儿童模式特殊判断，并用名称修改测试验证不可见配置不因改名失效

## 2. 游戏端编辑与列表

- [x] 2.1 在游戏名称/配置区域增加“儿童模式下可见”开关，验证新建游戏默认开启且旧游戏显示正确
- [x] 2.2 接入开关保存、取消和失败反馈，验证保存失败时界面不伪装为成功并保留原状态
- [x] 2.3 在儿童模式切换或收到同步状态后重新计算游戏列表，验证不可见游戏被隐藏、关闭儿童模式后恢复且顺序不变
- [x] 2.4 处理游戏选择界面正在打开时的配置变化，验证尚未启动的不可见游戏不能继续作为新局选择且已运行游戏不被中断

## 3. 后端目录与启动校验

- [x] 3.1 让可玩游戏目录根据全局 `childMode` 和游戏级 `childModeVisible` 共同过滤，验证开启和关闭儿童模式的结果
- [x] 3.2 在游戏启动入口复用同一可见性判定，验证绕过列表直接启动不可见游戏返回明确限制错误
- [x] 3.3 验证儿童模式关闭或游戏配置为可见时保留原有启动校验和启动流程

## 4. 跨端集成与回归测试

- [x] 4.1 扩展会员管理端儿童模式同步契约测试，验证游戏端重连后仍使用最新总开关状态
- [x] 4.2 增加配置默认值、列表过滤、名称修改、直接启动拦截和状态切换的自动化测试
- [x] 4.3 运行受影响的游戏端、游戏后端和平台测试套件，确认现有儿童模式、游戏编辑和游戏启动回归通过
- [x] 4.4 更新变更说明和测试记录，明确字段默认值、总开关与游戏级开关的组合规则
