## 1. 布局状态与校验基础

- [x] 1.1 在 `ledGame` 新增符号灯布局归一化模块，支持缺失字段补默认值且不覆盖已有值；用单元测试验证历史布局和空配置输入
- [x] 1.2 实现按 `wall|idx` 重建四面墙 rows 的纯函数，验证数量增加、减少和跨墙修改时已有 `align` 保留
- [x] 1.3 实现控制器/通道范围、重复通道、起点限制和 `数字,数字` 对齐坐标校验；运行布局状态单元测试确认非法草稿不能保存

## 2. 编辑器界面与预览

- [x] 2.1 在 `SimpleGameEditorView` 的共享配置区域增加“符号灯布局”按钮，并确认 `simple`、`normal`、`diffcult` 都能打开同一弹窗
- [x] 2.2 新增独立布局弹窗组件，完成启用状态、控制器、端口、方向、强制对齐、四面墙数量、起点和对齐输入控件
- [x] 2.3 实现本地预览绘制，显示地砖网格、四面墙、符号灯、圆灯和链路方向；测试点击“绘制”不产生 HTTP 或控制器请求
- [x] 2.4 处理弹窗加载、取消、校验错误和保存失败状态，验证失败时弹窗保持打开且草稿不丢失

## 3. 持久化与兼容性

- [x] 3.1 复用现有编辑器保存 helper，将布局作为 `commonConfig.pixelLightWiring` patch 保存，不新增接口或数据库字段
- [x] 3.2 验证布局保存不会覆盖未保存的矩阵/关卡编辑，并在成功后重新打开弹窗能够显示最新布局
- [x] 3.3 验证 `open = 0` 只关闭布局而不清除墙数量、rows、起点和对齐配置
- [x] 3.4 为后端编辑器文档增加/补充 pixelLightWiring GET→PUT round-trip 测试，确认旧游戏配置结构仍可读取和保存

## 4. 集成验收

- [x] 4.1 执行 `ledGame` 相关单元测试和构建，确认新组件不影响现有编辑器功能
- [x] 4.2 执行 `ledGame-backend` 编辑器测试，确认无数据库迁移且原有游戏保存、运行测试保持通过
- [ ] 4.3 手动验收三种默认玩法的布局编辑、保存、重新打开和关闭/重新启用流程，确认本 Change 不发送运行时灯光报文
