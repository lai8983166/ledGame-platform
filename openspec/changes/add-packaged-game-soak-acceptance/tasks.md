## 1. 范围核对与配置契约

- [x] 1.1 核对游戏端及后端当前自然终止原因、全局时长解析、生产形态替身和通信计数入口，平台仓库仅核对可复用测试工具；给出位置清单并在相关游戏仓库建立对应实施记录，禁止越过当前仓库范围直接修改
- [x] 1.2 先写配置/预检失败测试，覆盖空列表、重复/不存在/隐藏 ID、非法人数关卡、无限或无法确定的总时长、运行中内容变化；测试须证明不改游戏配置、不按名称替换
- [x] 1.3 建立独立 game-soak-acceptance 模块、配置模板及只读游戏列表/预检命令；通过契约测试并展示实际游戏 ID、有效时长与执行顺序，模板不包含冒充真实对象的 ID

## 2. 打包应用启动和 UI 循环

- [x] 2.1 复用所有权机制仅启动游戏端及自有后端，隔离游戏库/配置；测试其他两端未启动时能独立游玩、房间联动关闭、无中心授权/数据库依赖，并验证原文件未修改、端口占用明确失败及只关闭自有进程
- [x] 2.2 核对/补齐游戏模式轮播方向和选中 ID 的稳定选择器；通过前端测试证明只点击可见卡片/箭头且不影响原产品交互
- [x] 2.3 实现 Playwright 点击主入口、待机、人数、指定游戏、关卡和开始，随后自然结束后点返回待机；实际打包短测证明 game/touch 模式、每个配置游戏至少一局且没有 DebugPanel/直接开局停止调用
- [x] 2.4 补流程异常测试：20 秒退回、错误目标、未进入 RUNNING、非自然中止、意外排队和结束页卡死；断言不计成功、不强制结束补通过
- [x] 2.5 实现单调时钟总时长、列表循环及有界自然收尾，使用虚拟时钟验证 18 小时边界、当前局收尾、用户中止和至少一次列表覆盖；报告区分总时长与 RUNNING 时长

## 3. 无硬件开发与可选模拟输入

- [x] 3.1 复用/补充显式隔离的生产形态就绪及输出替身，完成不接地砖的打包游戏模式短循环；测试普通配置不能跳过真实硬件检查，real 不自动转 simulated
- [x] 3.2 在游戏后端先写注入访问及输入契约测试，再接入默认关闭、本机/会话限定、坐标和限额校验的 DOWN/UP 注入入口；验证真实输入和注入复用规则而非直接改积分
- [x] 3.3 验证真实/模拟同坐标按住隔离、快速 DOWN/UP、重复/迟到事件、结束及换局清理；测试模拟释放不释放真实按住，旧会话事件不能污染新局
- [x] 3.4 打包短测覆盖不注入自然超时和主动模拟踩踏两种情况，核对自然结束与来源统计；报告真实硬件栏仍为未验证

## 4. 可用性、内存与通信监测

- [x] 4.1 实现 PID+创建时间的进程树采样、应用/Java/窗口响应探测；用替身退出/卡死/子进程更换测试区分故障与正常清理，验证不自动重启掩盖失败
- [x] 4.2 实现每 10 秒私有内存/工作集采样、游戏端整体及 Electron/Java 分组汇总、预热后窗口趋势及固定 8192MB 峰值判定；用稳定、持续增长、超限和缺失采样序列验证结论，单列测试器资源
- [x] 4.3 在实际 SDK 收发/解析边界补最小真实通信计数及有界错误样本，通过故障测试验证发送失败/解析拒绝等准确计数；无协议支持的 ACK/丢包判断明确不可测
- [x] 4.4 通过 real 无踩踏零接收、模拟输入、计数重置和采样中断测试，确认没有硬件样本不变成硬件通过，不要求发现数等于配置控制器数
- [x] 4.5 实现滚动/限额日志、有限失败截图和磁盘不足提示；持续模拟大量事件测试验证内存/文件增长有界且缺证据不补零

## 5. 报告、便携交付与验收

- [x] 5.1 输出中文报告、配置/游戏内容快照、局记录、进程内存序列和通信摘要；报告快照测试覆盖通过、失败、中止及部分未验证，指标可追溯到原始证据
- [x] 5.2 打包独立便携命令行工具及所需自动化运行依赖，交付简洁中文说明；在无源码工作目录仅启动游戏端打包 exe 完成短测，检查工具包不含正常业务数据、不要求其他两端或厂家授权文件
- [x] 5.3 运行各受影响仓库聚焦回归、无硬件多游戏短测及计划/报告测试，记录命令和真实覆盖；严格校验 Change，短测不得勾选下一条
- [ ] 5.4 在真实控制器与现场尺寸配置下实际运行 18 小时，核验固定 8192MB 内存峰值上限、各目标自然结束、程序可用性和真实通信证据；未实际执行或存在不可测项时保留未完成/未验证，不用已有硬件短测替代
- [x] 5.5 简化真实地砖向导：自动读取游戏端 exe 同级 `elc408` 的 `conf.json`、`wiring.json`，从 wiring 推断地砖尺寸，保留隔离复制/哈希校验，并补齐向导、配置发现和错误提示测试
- [x] 5.6 简化游戏数据库来源：向导优先发现本机当前用户数据库并传入隔离复制，找不到时回退 seed，检测锁文件并补齐来源发现、回退和不修改原库测试
- [x] 5.7 增加 2 小时烤机档位，更新向导提示、中文说明和档位测试，保持 5 分钟、30 分钟及 18 小时档位兼容
- [x] 5.8 修复自然结束过渡态：允许 `STOPPING` 在结算宽限内过渡到 `STOPPED`，补充 UI 回合测试、规范说明并重新打包便携工具
- [x] 5.9 固定游戏端进程树私有内存峰值上限为 8192MB，取消增长趋势作为判定条件，补齐监测/报告/配置测试并重新打包便携工具
