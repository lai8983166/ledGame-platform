## 1. 特效配置纯逻辑

- [x] 1.1 在 `ledGame/src/lib/effectEditor.js` 建立特效颜色白名单、默认值和归一化函数，覆盖四种颜色及用户指定的精灵名称；用单元测试确认颜色切换后候选列表和首项选择正确
- [x] 1.2 实现坐标、尺寸、步长和精灵点阵边界校验，覆盖 1-based UI 坐标、起终点相同、非法步长、越界矩形和越界精灵；用单元测试确认错误字段稳定返回
- [x] 1.3 实现确定性轨迹采样，包含起点和终点并处理静止轨迹、步长大于 1、步长小于 1 和最大帧保护；用单元测试确认预览与生成共用同一位置序列
- [x] 1.4 实现矩形对象和精灵对象的帧展开、UID 复用、当前帧前插入和从当前帧开始叠加合并；用单元测试确认原帧保留、合并不删除既有对象和不足帧自动追加

## 2. 特效配置弹窗

- [x] 2.1 新增独立 `GameEffectDialog.vue`，提供颜色、起终点、尺寸、步长、插入/合并、使用精灵和精灵下拉框，并显示场地尺寸；用组件测试确认弹窗能打开、关闭和显示当前草稿
- [x] 2.2 接入现有精灵库列表，按颜色和名称白名单过滤并处理重复名称；用组件测试确认绿色“安全块”、蓝色“得分块”、红色 17 项和紫色“Double”显示正确
- [x] 2.3 实现颜色切换自动选中目标颜色第一个精灵、勾选使用精灵后显示下拉框、取消勾选后禁用下拉框；用组件测试确认切换不会保留上一颜色的精灵
- [x] 2.4 实现本地轨迹预览和错误提示，使用 300ms 定时器、配置改变从第 0 帧重播、精灵按点阵渲染；用组件测试确认确认/取消/卸载都会清理定时器且不产生网络或硬件请求

## 3. 游戏编辑器集成

- [x] 3.1 在 `SimpleGameEditorView.vue` 配置区增加“特效”按钮，并把当前关卡、当前帧、场地尺寸和精灵列表传入弹窗；用编辑器测试确认 simple、normal、diffcult 三种游戏都能打开同一弹窗
- [x] 3.2 将弹窗确认结果应用到当前 `document.value.levels[].frameList` 草稿并更新当前帧选择，取消或校验失败不得改变矩阵草稿；用回归测试确认未保存矩阵编辑和关卡切换内容不丢失
- [x] 3.3 复用现有 `createEditorPayload()` 和游戏保存链路，确认弹窗本身不调用特效专用接口，外层保存后重新读取仍能得到生成帧；用保存 mock 测试确认 payload 只包含普通 `frameList` 数据
- [x] 3.4 验证特效写入不会覆盖 `commonConfig.pixelLightWiring`，且符号灯布局弹窗仍能正常打开、保存和重新读取；用组合回归测试覆盖特效与符号灯布局先后操作

## 4. 验证与验收

- [x] 4.1 执行 `ledGame` 的特效纯逻辑、组件和编辑器回归测试，确认新增测试全部通过且既有测试无回归
- [x] 4.2 执行 `pnpm build` 和 Electron 打包前端构建，确认新弹窗、精灵筛选和帧生成代码进入生产资源
- [ ] 4.3 手动验收 simple、normal、diffcult：配置四种颜色特效、切换精灵、预览、取消、确认、外层保存并重新打开，确认未发送硬件命令且帧效果一致
