## 1. 密钥封装与备份格式

- [x] 1.1 定义厂家恢复信封、恢复请求和恢复响应的版本化 JSON/二进制契约，包含 `recoveryKeyId`、`keyId`、数据库实例身份、revision、摘要、请求编号和过期时间，并用契约测试验证必填字段、长度和未知版本拒绝。
- [x] 1.2 增加只包含应用公钥的恢复密钥配置，校验公钥格式和 `recoveryKeyId`；验证客户安装包和 Git 中不存在厂家恢复私钥。
- [x] 1.3 扩展正式备份生成，把当前门店数据密钥封装为厂家恢复信封并写入元数据；验证备份仍可用原 DPAPI 信封启动，且数据库、头像和敏感字段明文不被写入恢复信封。
- [x] 1.4 保持 `TEST` 备份使用隔离恢复材料并与 `PRODUCTION` 候选隔离；运行备份环境隔离测试，确认正式目录不会列出测试恢复材料。

## 2. 后端恢复请求会话

- [x] 2.1 实现仅出厂账号可调用的恢复备份预检和请求生成服务，复用本机访问、启动门禁、数据库完整性、schema、实例身份、revision、keyId 和恢复信封校验；为非出厂账号、旧备份和无效路径补充失败测试。
- [x] 2.2 生成一次性临时密钥对和绑定请求，将私钥以当前 Windows DPAPI 保护保存到临时恢复会话；覆盖过期、取消、重启恢复和成功后清理测试，确认日志不含私钥或数据密钥。
- [x] 2.3 增加恢复请求文件导出及恢复材料清单接口，只输出请求文件和密钥信封，不复制 `platform.db`、头像目录或 CSV；验证页面/API 返回的文件清单和摘要一致。
- [x] 2.4 为恢复会话增加过期、单次使用、请求编号绑定和并发互斥策略；通过重复提交、请求/备份不匹配和并发请求测试确认不会产生多个可用会话。

## 3. 厂家离线恢复工具

- [x] 3.1 建立独立的厂家恢复工具工程和跨环境启动脚本，使用厂家私钥解封恢复信封、验证请求绑定信息，并在内存中重新封装门店数据密钥；运行工具测试确认不需要读取数据库或头像目录。
- [x] 3.2 为厂家工具增加 RSA-OAEP 密文完整性校验、过期检查、keyId/实例/revision 匹配和明确错误码；测试错误材料不会输出数据密钥、明文临时文件或敏感日志。
- [x] 3.3 生成与请求临时公钥绑定的恢复响应，并支持响应编号、有效期和单次使用标记；验证响应只能被对应的新系统会话接受。
- [x] 3.4 完成厂家私钥外置和发布隔离检查：私钥仅能从厂家受控目录加载，不进入客户安装包、测试默认目录、源代码或打包产物；运行发布清单扫描确认无私钥泄露。

## 4. 新系统响应导入与原子恢复

- [x] 4.1 扩展后端响应预校验，验证响应格式和 RSA-OAEP 完整性、请求会话、实例身份、keyId、revision、元数据摘要、SQLite integrity_check、schema、唯一启用出厂账号和关键密文可解密性；为每类失败建立错误码和测试。
- [x] 4.2 根据恢复响应取得门店数据密钥，在当前 Windows 用户环境中生成新的 DPAPI 信封，并让数据库/头像候选使用该密钥通过完整性校验；测试不改变候选数据库密文。
- [x] 4.3 将响应导入接入现有维护状态、停止持久化写入、pre-import 回滚副本、同卷 staging、SHA-256 复核、原子替换和重启复验链路；覆盖运行中游戏/排队拒绝、取消、正常成功和旧账号重新登录。
- [x] 4.4 增加导入中断矩阵测试，在保存回滚副本、替换数据库、替换密钥信封、替换头像和重启复验阶段强制终止，验证下次启动不会使用半套文件且能恢复可用状态。
- [x] 4.5 实现恢复响应单次消费和成功/失败审计记录；测试重复导入不修改当前数据库、备份或维护状态，日志不包含密钥及会员明文。

## 5. 会员管理端交互

- [x] 5.1 在登录后的数据库备份/恢复区域增加“生成恢复请求”和“导入厂家响应”，仅出厂账号可见且后端再次校验权限；Electron IPC/HTTP 直调测试必须覆盖权限拒绝。
- [x] 5.2 实现恢复请求向导：选择本地备份、展示版本/实例/revision/摘要、生成请求文件和清单，并明确提示无需发送 `platform.db` 与头像；验证取消不会修改数据库。
- [x] 5.3 实现恢复响应向导：选择原备份和响应文件、显示恢复编号和覆盖警告、预校验进度、确认/取消和阶段性错误；通过桌面测试确认失败后仍能回到安全维护界面。
- [x] 5.4 增加恢复成功、请求过期、不匹配、缺少恢复信封、响应已使用、文件读写失败和营业流程占用等中文提示；运行 UI 文案契约测试确认不泄露密钥或明文。
- [x] 5.5 在请求成功、取消、响应导入成功和回滚后清理临时私钥、请求和响应会话文件；通过文件系统断言验证过期会话不会长期遗留。

## 6. 兼容、文档与打包

- [x] 6.1 保持旧 DPAPI 备份在原 Windows 用户环境中的导入行为，并明确旧备份无恢复信封时拒绝跨系统请求；运行旧备份回归测试。
- [x] 6.2 更新数据库备份/恢复中文说明、厂家工具使用说明和灾备操作流程，说明请求材料、厂家返回材料、恢复前置条件、数据覆盖和恢复码/私钥保管边界；检查文档与 UI 文案一致。
- [x] 6.3 将厂家恢复工具加入厂家专用打包产物，确认客户三端发行包不携带厂家私钥和恢复工具敏感配置；运行打包清单与启动冒烟测试。
- [x] 6.4 更新正式备份、数据库导入、权限、断电回滚和测试环境隔离的自动化测试，并运行平台后端全量测试、会员管理端桌面测试及现有核心流程测试。
- [ ] 6.5 在隔离临时 userData、临时 SQLite 和临时恢复密钥下执行一次端到端演练：生成新格式备份、生成请求、厂家工具响应、跨 Windows 用户导入、重启复验和重复响应拒绝；确认不触碰正式数据库或正式备份目录。

## 7. 消融收紧

- [x] 7.1 移除恢复响应中重复的绑定字段和重复签名层，改用请求会话绑定与 RSA-OAEP 密文完整性校验；同步更新工具、后端、桌面端、文档和契约测试。
