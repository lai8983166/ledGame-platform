## 1. 安全边界与失败测试

- [x] 1.1 建立十张现有业务表、敏感列、等值查询、日志和备份文件的读写路径清单，并用测试夹具扫描 SQLite/WAL/SHM、备份及 `server.log` 中的测试手机号/姓名/手环 UID，验证当前明文基线能够被测试检出
- [x] 1.2 先写三级角色能力矩阵的后端参数化测试和前端策略测试，覆盖出厂管理员、店长、店员、停用/删除账号及无身份请求，验证未实现时受限场景失败
- [x] 1.3 为旧 `OPERATOR` 迁移、账号逻辑删除、会员导出入口去重和三类 CSV 权限编写契约测试，验证既有核心流程机器接口被列入兼容范围

## 2. 加密与本机密钥基础设施

- [x] 2.1 实现版本化 AES-256-GCM 值信封和独立 HMAC-SHA-256 查询摘要，单元测试覆盖随机 nonce、上下文绑定、Unicode/空值、篡改、错误密钥和未知版本
- [x] 2.2 实现 Windows DPAPI CurrentUser 数据密钥提供器、原子密钥信封文件和 keyId 校验；使用可注入测试提供器验证开发/自动化隔离，并在 Windows 聚焦测试中验证重启可解封且文件/日志无明文密钥
- [x] 2.3 增加 `data_protection_state` 与启动维护错误码，验证密钥缺失、不匹配、DPAPI 失败和密文认证失败时禁止业务写入且不创建空数据库

## 3. SQLite 数据迁移与查询兼容

- [x] 3.1 先构造包含会员、手环、充值、绑定、多人游玩、操作日志和账号的真实结构明文数据库，测试迁移前后主键、关联、条数、积分、余额、修订号和密码哈希完全一致
- [x] 3.2 更新 schema 版本、敏感字段存储和 `phone_lookup_hash`/`card_uid_lookup_hash` 唯一索引，实施事务化明文迁移；用故障注入测试验证断电/异常中止后不会留下可营业的半迁移状态且可安全重试
- [x] 3.3 更新会员注册/查询/删除、手环查询/充值/绑定/清除/回收/解绑的数据访问路径，在进入 SQL 前计算摘要、返回响应前解密；核心流程集成测试验证重复手机号/UID仍被拒绝且自助注册端接口结构不变
- [x] 3.4 更新充值记录、绑定记录、游玩记录、排行榜、玩家信息和运营统计查询，验证统计字段无需全表解密、多人积分与现有业务结果不变
- [x] 3.5 运行 SQLite 并发写入和多点并发聚焦测试，验证加密/HMAC 不引入数据丢失、唯一性绕过、明显锁竞争或不可接受的响应退化

## 4. 日志保护

- [x] 4.1 加密 `operator_action_logs` 中的账号快照、目标和摘要，保持操作类型、内部 operatorId、targetType 和时间可筛选；审计查询测试验证授权读取可解密、删除账号后历史日志仍存在
- [x] 4.2 对服务器文件日志、异常处理和请求审计实施敏感值最小化/脱敏，测试正常、失败、越权和导出场景均不出现完整手机号、姓名、手环 UID、密码、数据密钥或 CSV 正文

## 5. 加密备份、启动核对与导入

- [x] 5.1 扩展备份元数据和候选检查以保存/验证 encryptionVersion、keyId 及 DPAPI 信封，备份集成测试验证 `latest`/`history` 数据库中无敏感明文且同 Windows 用户环境可恢复
- [x] 5.2 在覆盖主库前验证候选密文可解密和完整性，测试错误 keyId、错误 Windows 用户信封、篡改密文及损坏数据库均被拒绝且当前主库不变
- [x] 5.3 实现历史明文备份识别、迁移或 `PLAINTEXT_LEGACY_BACKUP` 隔离，测试目标盘离线不阻断密文主库营业、重新上线后处理旧备份并更新状态提示
- [x] 5.4 回归数据库修订号、身份冲突、启动门禁、异盘掉线重连和导入后重新登录，验证加密状态不会再次触发错误的空库或备份冲突

## 6. 三级账号与后端授权

- [x] 6.1 重建 `operator_accounts` 角色约束并增加 `deleted_at`，把 `OPERATOR` 原位迁移为 `CLERK`；迁移测试验证原密码、启停状态、创建关系和出厂管理员保护不变
- [x] 6.2 扩展账号 API 以创建指定角色并执行角色范围内的查看、修改、密码重置、停用和逻辑删除；测试出厂管理员可管理店长/店员，店长只能管理店员，所有角色可改本人密码
- [x] 6.3 在后端集中实现具名能力检查，并逐项覆盖会员删除、运营总览、房间、记录、排行、功能开关、账号、导出、数据库维护和服务配置接口；参数化集成测试验证每个允许/拒绝组合及明确 403
- [x] 6.4 明确并测试共享机器接口允许列表，运行自助注册、手环准入、游戏开始/结束/积分结算和玩家信息大型测试，确认不要求自助注册端或游戏端携带操作员账号
- [x] 6.5 将成功和拒绝的敏感管理操作写入最小化审计，验证店长删除店员是其唯一删除能力，会员删除仅出厂管理员可执行，手环清除/回收/解绑仍属于三种角色可用的手环状态操作

## 7. 会员管理端三级权限界面

- [x] 7.1 更新 API 客户端、OperatorProfile、账号状态和前端权限策略为 `FACTORY_ADMIN / STORE_MANAGER / CLERK`，镜像测试逐项对照后端能力矩阵
- [x] 7.2 按角色过滤导航和设置分区：店员仅见手环、会员、功能开关/本人密码；店长可见运营页面和店员账号区；出厂管理员可见全部维护区，组件测试验证不可通过残留路由显示受限内容
- [x] 7.3 更新账号管理界面以支持角色选择、目标范围、密码重置、停用和逻辑删除确认，分别用出厂管理员、店长和店员 UI 测试验证按钮与列表范围
- [x] 7.4 统一处理账号停用/删除及后端 403：清除失效当前身份或显示权限不足，不把权限错误误报为网络故障；桌面端焦点回归测试验证确认弹窗后输入仍可用

## 8. 解密导出与入口调整

- [x] 8.1 改造会员、充值和游玩 CSV 服务为后端逐行解密/有界输出，测试中文、逗号、换行、空值和大批量记录，确认文件可读且服务日志/默认临时目录没有明文副本
- [x] 8.2 将三类导出限制为出厂管理员和店长，测试店员、停用/删除账号和无身份请求均返回 403 且 Electron 不创建目标文件
- [x] 8.3 删除会员管理页重复的会员导出按钮与说明，只在“记录与数据”按页签显示三类导出；前端测试验证店长/出厂可见、店员不可见且保存位置仍由用户选择

## 9. 综合验证与交付

- [x] 9.1 运行会员管理端服务端、客户端、桌面端、核心流程、SQLite 并发、异盘备份和多点并发自动化测试，记录测试数量与结果并严格校验本 Change
- [x] 9.2 使用隔离用户数据目录打包会员管理端，验证首次明文迁移、重启读取、开发桌面版与打包版同库、同机备份导入、密钥缺失维护页及三个角色实际登录流程
- [x] 9.3 更新中文使用说明与维护说明，明确普通 CSV 是明文、系统备份是密文、当前仅支持同 Windows 用户环境自动恢复以及密钥丢失的处理边界

## 10. 消融与结构收紧

- [x] 10.1 删除没有合法生产用途的数据加密关闭开关，并保留自动化测试显式测试密钥作为唯一隔离入口
- [x] 10.2 删除前后端权限矩阵中没有任何接口或界面调用方的能力项，保留真正参与授权判断的最小能力集合
- [x] 10.3 缩减只在数据库导入准备阶段内部传递的 manifest 字段和仅供测试的备份元数据重载构造，并运行完整回归验证行为不变
