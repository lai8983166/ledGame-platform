## 1. Player Info 查询契约

- [ ] 1.1 扩展 Spring Boot `/api/player-info` 控制器和服务，使 `phone` 与 `wristbandUid` 必须二选一，并复用同一会员、积分、排名、余额和近期游玩记录投影；用服务端集成测试覆盖手机号、有效手环、未知手环、双参数和空参数场景
- [ ] 1.2 扩展 `packages/api-client` 的 Player Info 类型与客户端方法，保留旧手机号调用兼容性并生成稳定的错误码；运行 API client 单元测试确认请求参数和错误映射
- [ ] 1.3 确认手环查询为只读且只接受当前有效绑定，补充测试证明查询不会激活、开始计时、绑定或修改手环

## 2. 自助注册端会员信息查询

- [ ] 2.1 扩展 `playerInfoFlow` 支持手机号/手环 UID 两种输入模式、格式校验和清理；补充状态机测试覆盖两种成功、失败和重置路径
- [ ] 2.2 在 Player Info 输入页增加互斥查询方式选择、手环刷卡入口和取消路径，复用键盘读卡器但不调用绑定接口；通过前端契约测试确认输入和请求参数正确
- [ ] 2.3 完善结果页的积分总数、当前排名和近期游玩记录展示，处理空记录、缺少可选字段和错误状态；运行 kiosk UI 测试确认不显示伪造数据
- [ ] 2.4 修改离线遮罩文案，只保留会员管理端网络连接问题与检查网络建议，并测试断线/恢复连接时会话清理和恢复行为

## 3. 流程倒计时与设置

- [ ] 3.1 新增自助端倒计时配置模型与本地持久化，提供激活、刷环、成功页和 Player Info 流程的默认值、10–600 秒校验和损坏配置回退；补充配置单元测试
- [ ] 3.2 在标题栏 LED GAME 右侧增加设置按钮和设置弹窗，展示并保存各流程倒计时，保存失败或非法值时保留旧配置并显示错误；补充组件契约测试
- [ ] 3.3 实现顶层流程计时器，在进入每个非首页流程时重新计时并显示剩余秒数；验证返回、切换步骤、弹窗和读卡状态会取消旧计时器且不会产生重复计时
- [ ] 3.4 实现超时回首页事务，取消未完成读卡/API 请求、清理会员/手环/Player Info 临时数据，并以定时器测试和端到端 UI 测试验证超时不会产生数据库变更

## 4. 集成验证与交付

- [ ] 4.1 更新核心流程和 Player Info 集成测试，验证手机号注册、手环查询、积分排名、游玩记录、离线恢复和倒计时超时可以连续运行
- [ ] 4.2 运行会员管理端服务端测试、共享包测试、自助注册端测试和完整平台测试，所有测试通过后再生成测试报告
- [ ] 4.3 构建并检查自助注册端生产包，手动验收设置入口、两种查询方式、网络错误提示、倒计时归零回首页以及个人信息清理
